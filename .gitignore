/target
**/*.rs.bk
.DS_Store
docs/scratch/

# soak.sh halt-drill scratch state (pre-swap counter snapshot)
deploy/docker/.halt-preswap

# Phase B-WAN host addresses (generated by qumbra-deploy/terraform).
# A topology map of the live net — never commit.
deploy/hosts
full-suite.log
