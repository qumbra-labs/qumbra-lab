Multica QUM-178. Follow-up to PR #670's review: run `deploy/dry-run.sh` in CI, gated on `paths: deploy/**` (and, measured, `crates/qumbra-node/**`).

**Body is provisional — the CI runs this PR exists to demonstrate are in flight. Full report (decisions, measurements with their basis, CI verbatim, what was not verified) lands here once they finish.**
