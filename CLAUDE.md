# qumbra-lab

Prototype lab for **Qumbra**, a post-quantum privacy-chain design exercise. This file orients Claude Code sessions opened in this repo — read it fully before touching code.

## Ownership — read this first

- **This is Larry's personal project.** It is NOT related to Larry's contract work for pqabelian (Abelian/QDay/USD9-swap, which lives under `~/develop/bidot-blochains/`). Do not frame anything here as "propose to Leo/pqabelian"; Larry is the sole decision-maker.
- Repo is **private** (`lai3d/qumbra-lab`). Keep it that way; new sibling repos also default to `--private`.
- All Qumbra repos live under `~/develop/qumbra/` on disk.

## What Qumbra is, in one paragraph

A 2026 greenfield design for a post-quantum privacy L1: note-based UTXO, one global shielded pool (no transparent tier, no rings), one monolithic STARK per transaction (spend authorization inside the proof — no per-spend signatures), conservative hash (Keccak/SHA-class) everywhere in consensus, ML-KEM note encryption, hybrid PoW + small BFT finality committee. Deliberately NOT Abelian's all-lattice PQRingCT route — the design bets that hash/STARK + NIST standard parts beat lattice ring signatures on every engineering axis.

## Canonical design source

The eight design docs are the **binding spec** for this repo:

- GitHub: [`lai3d/qumbra-design`](https://github.com/lai3d/qumbra-design) (extracted 2026-07 from the bidot-blockchains-design notebook, history preserved)
- Local clone: `~/develop/qumbra/qumbra-design/`

Before touching the AIR, read at minimum: `transaction-model-and-anonymity-set.md` (the circuit's statement) and `performance-budget.md` (targets + the hash-choice framework). `benchmark-survey-2026-07.md` holds the sourced third-party numbers M1 validates against. Docs are EN with `-zh` pairs; EN is authoritative on technical details.

## Design decisions that bind this code

| Decision | Source | Consequence here |
|---|---|---|
| Fixed-shape 2×2-bucket circuit: 2 × depth-32 Merkle membership + 2 × nullifier PRF + 2 × spend-key knowledge + 2 × commitment well-formedness + in-circuit balance ≈ ~90 hash invocations | tx-model doc | `qlab-air` implements exactly this shape; no generality, no zkVM |
| Conservative hash (Keccak/SHA-class) everywhere in consensus; exact pick is **M1's question** | performance-budget §2 | bench matrix = Poseidon2 (calibration baseline only) vs Keccak-f vs SHA-256 vs BLAKE3 raw-AIR |
| Targets: tx ≤ 150 KB, prove ≤ 3 s laptop / ≤ 15 s phone, ~100-bit conjectured security | performance-budget §3–4 | every bench reports against these; the 15 s phone number is the design's riskiest and M2's whole job |
| Raw fixed-shape AIR, no zkVM | performance-budget §4 | the RISC Zero/SP1 7× spread on identical workloads is the zkVM tax we refuse to pay |

If a measurement contradicts a design-doc estimate, the doc gets a correction PR — the design repo has precedent for retracting its own claims (performance-budget §2 retracted tx-model's "layered hedge"). Never silently diverge from the docs.

## Milestones

- **M1 — DONE (2026-07-16, PRs #1/#2/#3 + qumbra-design's `prototype-bench-M1.md`)**: proving time is a non-issue (49 ms Keccak, 10–60× margin); proof size is the binding constraint. Keccak = only live conservative candidate (BLAKE3 eliminated by 173 KB zeta floor; SHA-256 has no AIR); narrow-layout geometry floor **172.5 KB** (U-curve @ 164 cols, probe calibrated −0.005%) vs 55 KB Poseidon2 vs ≤150 KB target.
- **M1.6 (a)+(b) — DONE (2026-07-16, `levers` bench mode; runs in `docs/levers-M1.6-run*.md`)**: the ≤150 KB target is REACHED with margin. FRI arity is the dominant lever: arity 8 alone takes the 164-col floor 172.5 → 101.6 KB; blowup 32 / 18 queries alone → 146.8 KB; combined b32/q18/a16 floor = **79.6 KB @ 41 cols** (reproduced twice, ±0.2 KB grind jitter). Higher arity flattens the fold-path term and moves the U-curve minimum narrower (L6 41-col becomes optimal), exactly as predicted. Prove stays ≤ 0.7 s (target 3 s). Levers (c) truncated digest / (d) WHIR are no longer needed for the target — optional future squeeze. → **M1.5b (correct narrow-Keccak AIR implementation) is justified and next**; results to be written up in qumbra-design's prototype-bench doc.
- **M1.5b — DONE (2026-07-17, `qlab-air::narrow` + `narrow` bench mode; runs in `docs/narrow-M15b-run*.md`)**: correct-semantics narrow Keccak-f[1600] at **371 cols × 3072 rows/perm** (vs 2,633-col stock; z-slice pipeline, 128 rows/round, three packed shift registers, 27 free periodic columns + 1 preprocessed RC column; semantics = reference chain, cross-checked vs p3-keccak). **Both targets met: 141.2 KB / 2.0 s prove @ b16/q19/g24/fp16/a16** (exactly 100 bits; 147.7 KB @ q20/g20), reproduced across fresh processes. Findings: the mock's sub-160-col rungs are NOT buildable (2-row window, no lookups — packed-carry floor + rho shift registers); real = mock + 11% entirely from the preprocessed RC column's per-query openings (~740 B/query — in-trace LFSR gadget is the identified next squeeze, ~−15 KB net); realized constraint density = 15× census (prove-time only, harmless at 2 s); b32/a16 = 139.3 KB but RAM-bound on 36 GiB (25 GB LDE). qumbra-design §9 correction owed: "41–82 cols" → 371 cols realizable, margin 6% not 47%.
- **M1.5c — DONE (2026-07-17, runs in `docs/narrow-M15c-run*.md`)**: preprocessed iota-RC column replaced by an in-trace rotating ring (24 registers + 7 exposed bits, first-row pinned, block-boundary rotation via free periodic flag; width 371 → 402, no preprocessed trace at all). **Robust double-pass: 136.9 KB / 2.0 s @ b16/q20/g20/fp16/a16, reproduced identically**; 130.8 KB @ q19/g24 (prove 2.2–3.3 s — the 2^24 grind is in prove time and has high variance); 129.0 KB @ b32/a16 (RAM-bound here). Net −10 to −13 KB vs M1.5b at every config. Conservative-route margin now ~9% at the robust point.
- **M3 — DONE (2026-07-17, `qlab-air` bucket builder + `bucket` bench mode; runs in `docs/bucket-M3-run*.md`)**: the complete 2×2 transaction statement (2× [nk/rkm derivation + nullifier + cm opening + depth-32 shared-tree membership] + 2 outputs + in-circuit balance + public binding of anchor/nf/cm'/fee) over the narrow-Keccak pipeline. **Both gates pass at the decided consensus config: 136.4 KB (fixed-width wire format) / 1.6 s prove**, reproduced twice; 130.7 KB @ q19/g24. Findings: the bucket is **83 perms → 2^18 rows** (not the budgeted 96/2^19 — height and prove time halved); **postcard varint is a codec artifact** (±18% by value density — bench reports postcard + bincode-fixed, gates use fixed); final width 618 cols; equality banks (2×16) bind cross-perm witness reuse, bind bank (16) closes digests against public values with an epoch one-shot killing program-replay in padding; balance = exact 16-bit-limb borrow chain. 18 tests incl. full-bucket positive w/ real PVs + wrong-nf/fee/anchor negatives. Bonus: at 2^18 the M2 ladder improves (b8 = 171.6 KB / ~5.2 GB LDE; b32 = 128.3 KB / 3.1 s). Written up in qumbra-design `prototype-bench-M1.md` §10. **Next per the design repo's `ROADMAP.md`: M4 (aggregation rung 1) — design doc FIRST, in qumbra-design, before any lab code**; M2 device steps remain blocked on the user's Xcode/iPhone setup.
- **M2 — step 0 DONE (2026-07-17, `docs/m2-iphone-plan.md` + `docs/narrow-M2step0-*.md`)**: the 15 s target is safe (low-blowup configs prove in 0.65–1.1 s on the Mac → ~3–9 s phone-projected), but **memory is the real wall**: peak RSS 15.1 GB @ b16 (no iPhone), 7.6 GB @ b8 (marginal even on 12 GB devices), 3.9 GB @ b4 (fits 8 GB-class w/ entitlement) — and every phone-fitting config breaks ≤150 KB (b8 = 172.7 KB, b4 = 238.5 KB; monotone frontier, reproduced twice). **The self-proving-phone vs ≤150 KB collision is M2's actual finding** — resolution options (relax target for self-proved / delegation / WHIR / multi-config) belong to qumbra-design. Build path researched: cargo-dinghy first, hand-rolled bundle + `xcrun devicectl` fallback (entitlement control); `narrow --only <cfg>` filter added for per-launch device runs. Steps 1–2 (device harness + on-device matrix) await the user's iPhone/Xcode setup.
- ~~M2 original framing~~: go/no-go on the 15 s target — answered YES at step 0; fallback ladder in performance-budget §4 is now about *bytes vs RAM*, not seconds.
- **Branch decision (2026-07-17, qumbra-design `self-proving-vs-proof-size.md`): (b)** — consensus config = **b16/q20/g20/fp16/a16** (136.9 KB / 2.0 s); phones launch watch/sign; phone self-proving waits on WHIR; (a) b4/241 KB is the standing fallback. M2 device steps de-prioritized (fallback constants only). **Next: M3 — the full 2×2 bucket circuit** (2× depth-32 Merkle membership + 2× nullifier PRF + 2× spend-key + 2× commitment well-formedness + in-circuit balance over the narrow-Keccak primitive), must meet ≤150 KB / ≤3 s laptop at the decided config.
- **M4 step 0a — DONE (2026-07-17, `m4census` bench mode; runs in `docs/m4census-step0a-run*.md`)**: the verifier hash-workload census — counting adapters around every Keccak entry point of the verification config, no circuit built. **Measured: verifying one M3 consensus proof = 2,233 keccak-f** (compress 1,520 + leaf-sponge 540 + challenger 173; 22.4 KB transcript), deterministic across passes and runs, transcript identity asserted vs the plain config — the design doc's ~3,000 [derived] was 26% conservative. Wide-lane feasibility at that workload (2,233 perms → 2^16 wide rows): **b4 = 456 ms / 2.87 GB peak RSS / 593 KB fixed**, b8 = 842 ms / 5.7 GB, b16 = 1.56 s / 11.4 GB — all far inside aggregation-rung1 §6's ≤10 s / ≤32 GB leaf envelope. Interior-node fixed point: verifying a wide proof costs 3,560 (b16-committed) to 6,083 (b4) keccak-f — the tree converges; a16 vs a1 halves the compress term (5,400 → 2,040 at b4). Caveats: prices the hash workload only — constraint-eval/fold-arithmetic glue is **step 0b (the actual verifier circuit), next**; prove is not run-deterministic (parallel-grind PoW jitter, known since M1.6). Design-repo measured-update owed to aggregation-rung1 §4/§6.
- **M4 step 0b(i) — DONE (2026-07-17, `m4price` bench mode; runs in `docs/m4price-step0bi-run*.md`)**: the verifier ARITHMETIC census — the glue half that step 0a's hash census left open. Exact constraint-DAG walk (Arc-memoized): 873 constraints, 3,791 unique ops (1,953 mul), sharing 2.3x; counted-from-shape totals: **28,800 ext-mul + 28,663 ext-add + 60 ext-inv per verified proof**, dominated by reduced openings (25,000 = 20 q x 1,250 opened values), NOT by constraint eval or FRI folds. Priced at a bracketed arithmetic-bank cost: **glue = 0.6–1.1% of the hash workload's 141M cells** — aggregation-rung1 §4's +30–50% allowance was ~30–50x conservative, and even a 15x realization miss (the M1.5b density precedent) stays at 17%, inside the allowance. **The wide-lane leaf projection from 0a (456 ms / 2.87 GB at b4) effectively IS the verifier cost.** Remaining for step 0b(ii): the actual verifier circuit (arithmetic banks, injection routing, ext-inv gadget) — now an engineering task, not a feasibility risk. Design-repo measured-update owed.
- **M4 step 0b(ii) anchor + layout — DONE (2026-07-17, `m4anchor` bench mode; `docs/m4-verifier-circuit-layout.md` + `docs/m4anchor-step0bii-run*.md`)**: the ext-mul bank is REAL and measured — 12 cols x 2^15 rows, one deg-4 ext-mul/row (W=3, 4 degree-2 constraints, values cross-checked against p3's ext arithmetic): 32,768 muls prove in **36–107 ms** across lane configs, ~91 KB fixed at b4, no quotient surprises. Verifier-rectangle layout fixed: **2,673 cols x 2^16** (keccak lane 2,633 + mul bank 12 + add bank 12 + ~16 routing) = **+1.5% cells over the 0a baseline -> projected b4 leaf ~463 ms / ~2.91 GB** (rectangle-to-rectangle accounting; the 0a baseline was already 2^16-padded). Remaining unknowns for the full build: the FS byte-packing gadget and injection routing (~16-col allowance); banks + keccak schedule are mechanical after M3. **Next: the 0b(ii) build itself (~8–15 session-hours)**; tree prototype (step 1) after it reports.
- **M4 step 0b(ii) increment 1 — DONE (2026-07-17, `m4skel` bench mode; runs in `docs/m4skel-step0bii-run*.md`)**: the composite verifier rectangle SKELETON — keccak lane (stock p3-keccak-air at 2,233 perms, evaluated unmodified through a column-offset **LaneBuilder** adapter) + ext-mul bank (28,800 rows) + ext-add bank (28,663 rows) coexisting in one 2,657-col x 2^16 AIR. **Measured: b4 = 476–561 ms / 3.58 GB / 597.9 KB fixed** (projection was ~463 ms / ~2.91 GB; +0.69 GB is the trace-composition transient, removable by fusing generators), b16 = 1.44–1.61 s / 12.2 GB. Two build lessons: full-width Expr collect in eval costs +70% prove (runs per LDE point — narrow to needed columns); trace buffers need up-front LDE capacity (late reserve realloc = 3x RSS). Remaining (increments 2+): cross-lane routing, FS challenge extraction, public binding — the LaneBuilder composition mechanism is proven.
- **M4 step 0b(ii) increments 2+3 — DONE (2026-07-17, `m4route` bench mode; PR #16; runs in `docs/m4route-step0bii-run*.md`)**: injection routing collapsed to step-flag-muxed same-row equality (overwrite-mode sponge + 24-row preimage replication — no accumulator windows needed); FS byte-packing gadget real and **cross-checked against a native SerializingChallenger32 run** (2 rows/draw, 12-draw capacity, reject-redraw comparator, all deg ≤ 3). Col allowance honestly overshot: ~16 → 28 (16 are no-lookup byte-range bools), +0.45% cells, prove-time free. b4 = 455–497 ms / 3.61 GB / 603.1 KB fixed; b16 = 350.6 KB fixed. 14/14 tests incl. 4 negatives. Gate cols still witness this round.
- **M4 step 0b(ii) increment 4 — CORRECTNESS MILESTONE DONE (2026-07-18, PR #17; stages 1/2/A–L across 3 relay sessions; `crates/qlab-bench/src/m4gate.rs` + `docs/m4gate-inc4-status.md`)**: the verifier gate rectangle (3,532 cols × 2^16) proves-in-circuit a REAL M3 consensus proof, and **all four gate-exit negatives bind** (wrong-root / tampered-opening / wrong-challenge / bad-fold all UNSAT), plus 6 fold-pipeline internal negatives; 40/40 crate tests (21 m4gate), independently reproduced. FS/challenge/draw-schedule + full ext-arith fold pipeline bound. **This is correctness, NOT the calibration-gate exit** — two things remain before bench/exit: (1) **degree reduction ≤3** — the rectangle is stuck at deg-6 (pre-existing flush-automaton debt, constraint #4165; the new fold arith is only deg 2), which BLOCKS bench, and the whole point of step 0b(ii) is a *measured* leaf ≤10 s / ≤32 GB; (2) **fold-chain endpoint value-pins** (M_RO/PZACC start, M_HORN/FPREG end) for full end-to-end fold soundness — bad-fold today binds single-cell tampering because RUNEV is threading-pinned + consistency-anchored, but a coordinated internally-consistent wrong chain isn't yet excluded. Smaller remainders: canonicity comparator wired into eval (filled-but-unreferenced), public-surface running-digest, FSGATE schedule position.
- **M4 step 0b(ii) — CALIBRATION GATE DONE (2026-07-18, `m4gate` bench mode; branch `claude/m4-gate-exit`; runs in `docs/m4gate-step0bii-run{1,2}.md`)**: the verifier gate rectangle is finished and **measured**. (1) **Degree reduction ≤3** — all ~400 deg 4/5/6 constraints (flush automaton + query selectors + fold pipeline) factored through ~90 materialized product columns filled by a `fill_derived` post-pass; **max constraint degree 6 → 3**, unblocking the consensus-quotient-degree bench. (2) **Endpoint value-pins** — both fold-chain endpoints value-pinned: START = PZACC/PREG accumulation + A/P/PX0 captures + M_RO 9-row reduced-opening assembly; END = FPREG capture (FPI counter) + M_HORN Horner (`RUNEV == Horner(final_poly, x_fin)`). **Full end-to-end fold soundness** — 9 tamper negatives bind (4 gate-exit + pzacc/preg/capture/fpreg/mro); 46/46 crate tests. Rectangle 3,532 → **3,626 cols × 2^16, 2,382 lane perms**. **Measured leaf (reproduced twice, Apple M5 Max/36 GiB): b4/q40 = 0.67 s / 11.9 GB / 779.6 KB; b16/q20 = 2.15 s / 16.3–17.3 GB / 453.6 KB** — vs aggregation-rung1 §6's ≤10 s / ≤32 GB leaf envelope, **PASS on both dimensions with 4.6–15× time and 1.9–2.7× RAM margin**. Well inside (not "outside by ≥2×"), so **M4 step 1 (tree prototype) is unblocked; no design-doc re-open**. Residual (not soundness-blocking, for a later pass): canonicity `< p` comparator still filled-but-unreferenced; public-surface running-digest; FSGATE schedule-position pin.
- **M4 step 1 stage 1 — DONE (2026-07-19, PR #19; `m4treerec` + `m4tree` bench mode; runs in `docs/m4tree-step1a-run{1,2}.md`)**: interior-node recorder + census. The interior node verifies a proof of the *leaf's* shape (2^16 × 3,626 wide-committed), not the M3 narrow proof — so a fresh recorder (`walk_with_cfg` extracted from m4gaterec, M3's `walk()` now a one-line wrapper; walk-vs-native cross-check still green) records the leaf-verification Schedule the interior circuit must consume, cross-checked vs native `verify()` (`recorder_accepts_leaf`). **Measured: verifying ONE child leaf = 7,503 keccak-f** (leaf-sponge 4,560 + compress 2,040 + challenger 903) — **+23% over m4census's 6,083** (census undercounted leaf-sponge: a wide-proof query opens two full 3,626-col rows = 7,260 values); **reduced-opening 290,400 values/child (40 q × 7,260) ≈ 11.6× the leaf's 25,000**. These two are the concrete drivers of the ≤32 GB interior risk. Also landed the permanent `assert!(max_degree ≤ 3)` guard (`constraint_degree_within_budget`, closing PR #18's print-only soft spot). 48/48 crate tests. Design-repo measured-update landed (aggregation-rung1 §4 + ROADMAP, PR-side).
- **M4 step 1 stage 2+3 — DONE (2026-07-20/21, PR #23, 5-leg relay on `claude/m4-tree-step2`; runs in `docs/m4interior-stage3-run{1,2}.md`)**: the interior verifier CIRCUIT + the RSS gate. Stage 2: row-stacked two-child interior at constant width (leaf-shape convergence — parallel-columns would double width per level and break the aggregation fixed point): `emit_child` extraction (byte-identical regression-guarded), csel child-boundary re-anchor (constraint-pinned, 250 conflicting carries resolved), per-child opvs routing (chi/cc), **distinct children L≠R (the hard PR-gate)**, keccak-merge root (capacity chain + dL/dR carry + root squeeze; **named residual issue #24**: merge preimages are witness, not pv-bound — msh columns or consumer-side native-check invariant before productionization). Leaf gate widened 3,626 → 3,638 (+12 boundary cols; supersedes PR #18's width). **63/63 unfiltered suite**, deg ≤ 3 held (narrow + interior), 13+ tamper negatives incl. per-lane and merge region/chain/bind. Stage 3 measured per the pre-registered §7.1 protocol (aggregation-rung1): **two-child b4/2^19 peak footprint 30.42 GB (reproduced 3× incl. coordinator-independent, zero swap)**; b2/q80 substitute 20.70/20.53 GB / 4.9 s / 1.51 MB; canary 15.22 GB @ 2^18. Metric finding: at b4/2^19 macOS memory compression makes max-RSS non-reproducible (16.4–23.4 GB) — **peak footprint is the faithful metric**; prove-time under compression (42–166 s) is noise, clean b4 ≈ 13–15 s. **§7.3 verdict (coordinator, 2026-07-21): PASS — 30.42 ≤ 32 GB** (5% margin; compressor engages on the 36 GiB rig; b2/q80 flagged as possibly the better aggregation-lane operating point — open). → next: tree/root assembly + epoch supply-attestation rider.
- **M4 step 2 — DONE (2026-07-21, PR #25, single leg; runs in `docs/m4tree-step2-run{1,2}.md`)**: end-to-end two-level tree assembly + the epoch Σfee rider. `m4assembly --lane b4|b2`: 2 DISTINCT M3 tx proofs → 2 leaf proofs (serial, native-verified, dropped) → interior root proof → full native-verify chain **with issue #24's consumer invariant as hard assertions** (`root == keccak-merge(opvsL,opvsR)` and `Σfee == feeL + feeR`, recomputed from the verified children — `consumer_fee_ok`). Rider: M3 fee = pv tail (`PV_FEE..PV_LEN`) → opvs-half tails → root exposes bound Σfee (deg-1 constraint at the root perm's last row); **honest finding: the rider inherits issue #24's binding boundary** — a consistent (fee+Σfee) tamper is SAT at circuit level, pinned as the explicit `interior_epoch_fee_boundary` test; Σcoinbase is block-level (M6). **68/68 unfiltered** (coordinator-reproduced); measured (×2 + coordinator rerun, zero swap): whole tree b2 = 12.4–15.6 s / interior 20.5 GB · b4 = 53–74 s (compression noise) / interior footprint **30.42 GB unchanged from PR #23 → the rider is footprint-free**. Operating-point data for the open §7.3 lane choice: b2/q80 buys ~10 GB headroom + ~4.8 s clean interior at 1.51 MB proof. **M4 rung-1 prototype (2-level) is COMPLETE.**
- **Issue #22 (B′ + lane flip) — DONE (2026-07-21, PR #26; runs in `docs/issue22-rebench-run{1,2}.md` + `docs/issue22-soundcalc.md`)**: grind 20 → 22 in all three lanes (consensus b16/q20/g22, leaf b4/q40/g22, interior b2/q80/g22 — the m4gate GRIND_BITS now DERIVED from CONSENSUS_CFG, not hardcoded), interior default lane → b2/q80 (b4 stays a flag). **B′'s size-invariance claim proven byte-for-byte** (consensus 139,721 B identical at g20 vs g22, same-width twin comparison); grind is RAM-neutral (interior footprint 18–20.5 GB band, zero swap) and its cost is prove-time with a HEAVY TAIL (interior g22 observed 7.2–29 s across runs, expected-case ~7–9 s — all inside ≤30 s; M1.5c's grind-variance finding, now quantified at 2^22). soundcalc (rev 809896fb) full proven-regime term inventory on record: totals 57–60 (the known conjectured-to-proven gap), no non-query term below the query floor, commit terms confirm the ~80-bit field cap; new fact — the consensus lane's JBR minimum is the BATCHING term (60), not query (61). 68/68 unfiltered, coordinator-reproduced.
- **M5 note-encryption prototype — DONE (2026-07-21, PR #28; runs in `docs/m5note-run{1,2}.md`)**: new isolated crate `qlab-note` (strictly additive; zero touches to m4*/qlab-air) + `m5note` bench module. ML-KEM-768 (`ml-kem` =0.3.2, FIPS-203-final, ACVP/Wycheproof KATs; **unaudited — libcrux-ml-kem named as the production alternative**) + ChaCha20-Poly1305 (=0.11.0). Compact-entry wire per the RATIFIED note-discovery Decisions 1/2 (cm 32 | ct 1,088 amortized per (tx,recipient) | tag 8 | clue 1 versioned-empty); domain-separated tag `Keccak256("qumbra:note-detect:v1" ‖ K ‖ cm)[..8]`; cm recompute **regression-locked byte-for-byte to qlab-air's `build_bucket`** (not a fork). Both scan paths behind a flag: FullFo default, FoSkip EXPERIMENTAL (true CPA-decap impossible at this crate's API — path (b) is an authenticity model + decomposition measurement: **~82% compute saved (upper bound) vs the doc's 40–50% estimate — recorded as an observation, not a doc correction**, pending a real CPA-decap). Measured (×2): 1,129 B/note (1-of-1) and **585 B/note (2-of-1) vs the doc's ~600 B ✓**; decap 18.5 µs ≈ 54k/s single-core. **75/75 + 21/21 green, coordinator-reproduced. The ANON-CCA written argument stays OPEN and named (design-repo deliverable).**
- **Issue #21 (leaf residuals) — DONE (2026-07-21, PR #29)**: R1 canonicity comparator wired into eval (range-forced 32-bit digit split, +34 cols, `gate_neg_noncanonical_word` UNSAT) + R3 FSGATE schedule pin (constraint-only, `gate_neg_fs_row_moved` UNSAT); 70/70 unfiltered; leaf 788.2 KB (+0.82%), prove/RSS unchanged, narrow byte-identical. **R2 (public-surface digest) re-scoped into #24** — empirically confirmed the inner PVs are unbound (tamper_coverage SAT-MISS probes committed); an output-only digest would be hollow.
- **Issue #24 D0–D2 — DONE (2026-07-22, PR #30)**: msh one-hot selector ring (positively pinned; +54 wide-only cols ≪ +91 budget) closes merge-preimage→pv and Σfee-input bindings at constraint level — root binding upgraded from computational (preimage-resistance caveat) to unconditional; probe evidence: SAT-MISS→UNSAT flips + `interior_epoch_fee_boundary` INVERTED (now asserts UNSAT). Interior b2 footprint 19.72 GB; leaf byte-identical; 79/79. ≥3-level trees UNBLOCKED. **Remaining D3 (leaf F0 digest input binding): XOR-mode challenger, cascades leaf public surface — spec in docs/issue24-findings.md; gates any leaf-digest exposure. Gotcha for the record: a builder subagent edited the MAIN worktree by mistake (self-caught, moved to its worktree, main restored) — re-affirms the isolated-worktree rule.**
- **M6 devnet — DONE (2026-07-22, PR #31)**: qlab-devnet crate, 棒 0–5 complete — placeholder-PoW chain, ML-DSA committee checkpoints, Ebb-and-Flow degradation, multi-node sim, real-M3-proof block validation; placeholder constants confined to params_devnet.rs (bannered); 59/59 devnet + post-merge main 80/80 green.
- **M7 wallet core — DONE (2026-07-22, PR #33)**: qlab-wallet — key hierarchy byte-locked to the circuit (nk/rkm/nf regression locks vs build_bucket), bech32m addresses (raw 1,233 B = 1+16+32+1184 / 1,985 chars / short 35 chars, CI-locked), fvk/ivk type-level capability split (compile_fail-proven), end-to-end derive→encrypt→scan→spend green. 198 workspace tests. rkm ek-only diversification per Option-1 (issue #32 = the circuit fix). Parked: HD-seed format, diversifier management. **Gotcha REPEAT: second builder main-worktree edit (self-caught) — subagents MUST verify cwd is their worktree before every edit batch.**
- **Compact-block server — DONE (2026-07-22, PR #34)**: qlab-cbserver — interop-spec §2 reference implementation, golden-bytes-locked framing (+1.5 B/note over the 585 B wire ✓), three endpoints over localhost, light-client scan with decoy-fetch hook, 24/24 planted notes found, frontier reconstructs roots; 223/223 workspace. Frontier serialization = reference proposal for spec O5.
- **Issue #32 (rkm‖d) — DONE (2026-07-22, PR #35)**: rkm = H(nk‖D_R‖d) at ZERO marginal cost (reused ARKM witness lanes 5-6: 0 perms/height/size — 139,721 B unchanged ×2); wallet locks broke-and-relocked; unlinkability proven end-to-end; address-gen now an fvk capability (compile_fail-locked); ADDRESS_VERSION stays 1. 227/227. **Diversified addresses are now actually unlinkable — the M7/tx-model §4 gap is closed.**
- **qlab-econ emission simulator — DONE (2026-07-22, PR #36)**: Monero-class closed-form family (reward = pure function of cumulative issuance, fixed tail floor) + C∞ contrast; exact S(h) (the audit anchor, tested to-the-coin); 15-candidate sweep over {half-life × tail-target × block-time} at 50-yr horizon in docs/econ-sweep.md; four §1 job-checks as asserting tests w/ negatives. All 15 pass by construction — the decision lives in the trade-offs (half-life = the economic knob; tail-target = the inflation floor; block time ≈ invariant; r0 = denomination). Nothing decided — feeds the coordinator's consensus-parameters appendix. 238/238.
- **Wallet demo — DONE (2026-07-22, PR #37)**: qlab-demo composes ALL FIVE crates (wallet+cbserver+devnet+air+note) — Alice pays Bob a real M3 proof → node validates → Bob scans the real socket-free cbserver client path (scan_local shares the socket path core) → detects (== sent) → spends (2nd real proof) → double-spend rejected → supply consistent. Byte-tight seam: proof cm_out[0] == compact-entry cm == Note::commitment(). +8 tests (246 workspace after the #36/#37 members-list merge). First whole-stack composition. Two follow-ups filed: F1 qlab-consensus extraction ([#38](https://github.com/lai3d/qumbra-lab/issues/38)), F2 anchor-to-live-tree binding ([#39](https://github.com/lai3d/qumbra-lab/issues/39)). *(Merge gotcha: PR was branched pre-#36; members-list conflict resolved by coordinator, union kept — always probe mergeStateStatus before deleting branches / writing milestone commits.)*
- **Selective disclosure-proof STARK — DONE (2026-07-22, PR #40)**: qlab-disclosure — the wallet-interop §3 envelope's proof, single-note statement (cm opens to (value, A-material) ∧ addr_commitment = Keccak256(to_raw_bytes)), rkm cross-binding proves "this payment went to THAT address"; narrow-Keccak reuse, 594 cols × 2^16, deg ≤ 3; §3 envelope serialize/verify (reject-unknown-ver/type) + tamper negatives + end-to-end with qlab-wallet. **Honest finding: proof is COMPARABLE to the 2×2 bucket, NOT ≪ it — 121.9 KB fixed (0.89×), floor 114.6 KB** (the addr_commitment over the 1,184-B ML-KEM ek is a 10-block sponge; small STARKs pay FRI fixed costs) — interop-spec O2 sizing note revised. +20 tests (266 workspace). Precondition (post-#32 derivations) + STOP-POINT (both packings cross-checked) cleared.
- **B″ batch — DONE (2026-07-23, PR #47, issue #41)**: queries 20→21 / 40→43 / 80→86 in all three lanes, restoring ~100-bit conjectured under the 2025/2197-corrected (base-field-entropy) accounting — 100.6/101.6/100.2. Both pre-registered fit-checks PASS as permanent tests (leaf 2^16 @ 91.0 %, interior 2^19 @ 82.3 %); gate schedule/shape fully re-derived data-driven (GATE_WIDTH 3672→3675, census 2,233→2,336 with per-term arithmetic); query counts derived-not-hardcoded (NQ from CONSENSUS_CFG, demo copy value-locked). **Measured: consensus wire 145,609 B / 142.2 KB (≤150 KB ✓, byte-identical ×2 + coordinator rerun), interior b2/q86 20.2–20.9 GB builder / 22.5 GB coordinator-independent (busy-machine inflation; ≤32 GB ✓, 0 swaps)**. 268/268 unfiltered, coordinator-reproduced. Residuals: stale "b16/q20" gates display string (cosmetic → #38 batch); qlab-disclosure reference config left at q20 (coordinator call: agreed — standalone statement, bump would stale its measured 121.9 KB for no security gain); b4-fallback two-child at q43 not re-measured solo (flagged for next bench batch).
- **Devnet load harness — DONE (2026-07-23, PR #46, issue #42)**: weight.rs two-median + quadratic penalty (all constants tunable, boundary negatives), seeded SplitMix64 sims — **byte-for-byte deterministic (coordinator double-run diff-identical)**. Findings: §6 two-median governor is demand-adaptive and does NOT bound a budgeted flood (governed = governor-OFF at launch; the §5 fee floor is the real anti-spam control; governor = anti-gigantism 2M cap + surge damping); §2 maturity ~100–150 blocks covers q ≤ 0.40 at realistic stalls (worst pathological point 118); §4 false jails a non-constraint to 80 % uptime → pick for detection speed. 103/103 devnet, 310/310 workspace, coordinator-reproduced incl. headline numbers exact.
- **Wallet HD-seed + diversifiers — DONE (2026-07-23, PR #44, issue #43)**: versioned MasterSeed → sk via domain-separated Keccak chain (golden byte-frozen + independent hand-recomputation locks), BIP-39-STYLE mnemonic (canonical 2048 wordlist; **Keccak checksum — deliberately NOT stock-BIP-39-recoverable, coordinator-ratified**: foreign-wallet "compat" would derive garbage anyway; one-hash-family + self-identifying artifact wins), DiversifierLedger (rotation, reuse/collision negatives), end-to-end seed→spend lock vs build_bucket. ADDRESS_VERSION stays 1; new SEED_VERSION/LEDGER_FORMAT_VERSION (coordinator-ratified). 287/287, coordinator-reproduced. Interop-spec §5 seed-format proposal in docs/ awaits coordinator (design-side).
- **Live-anchor binding — DONE (2026-07-23, PR #45, issue #39)**: build_bucket split into witness-taking core + fabricating wrapper (public signature unchanged); MerkleWitness (depth-32 fold_root); cbserver auth_path; devnet anchor gate (finalized-only + ≤24 h window, fresh/expired/nonfinal negatives); wallet/prover live_witness; demo spends against real finalized roots. Merged-tree integration verified by coordinator (**340/340 workspace** with q21+HD-seed+anchor all composed). **Acceptance fix (coordinator, direct-to-main ba8988a): MAX_ANCHOR_AGE_BLOCKS 1,440 → 1,152** — the builder used the pre-decision 60 s block time; at the DECIDED 75 s (params §2) 1,440 = 30 h would violate the ≤24 h anchor policy; 1,152 = exactly one epoch.
- **M9-N1 — DONE (2026-07-23, PR #56, issues #48+#38)**: qlab-consensus single-source crate (CONSENSUS_CFG frozen point, prove/verify wrappers, 145,609-B wire pin as permanent test; bench −209 lines; stale q20 strings fixed) + qlab-node skeleton (3 swappable store traits + NodeState + apply_block, atomic snapshots, open==replay restart-safety proven, unfinalized-anchor + cross-block double-spend negatives). Coordinator-accepted via sonnet T-verify (first full three-tier acceptance). Deferred: b4-fallback two-child re-measure (quiet rig owed).
- **M9-N3 — DONE (2026-07-23, PR #55, issue #50)**: real PoW — new dependency-light `qlab-pow` crate (light-mode RandomX over randomx-rs 1.4.1, pinned to the four official tevador/RandomX reference vectors + flag-independence proof; Monero-shape key-block rotation, division form proven bit-identical to the `rx_seedheight` mask; Zawy LWMA-1 in exact u128 integer form — on-target fixed point, 6T + low-L clamps, recency weighting, all as asserting tests) + devnet integration (`PowEngine::pow_hash` gains the key-block seed — KeccakPow ignores it, byte-identical to M6 so all M6 tests stay regression guards; `expected_difficulty` = LWMA-120 over the trailing window with a pure-LWMA wiring cross-check test; `pow_seed` resolves the key per-branch; 3 RandomX e2e incl. rotation + seed-sensitivity). Params discipline: 75 s cited FROZEN (params §2); LWMA window/clamps + key epoch/lag **testnet-tunable NOT frozen** (→ full-M8 v1.1). Fork-choice test rewritten (under LWMA longer ≠ heavier; heaviest-chain property itself unchanged). Builder honestly deferred the full unfiltered suite (N2's concurrent RSS measurement); coordinator ran it on the merged tree: **374/374 workspace, 0 failed** (sonnet T-verify evidence + coordinator diff adjudication — second full three-tier acceptance). Coordinator resolved the members-list DIRTY conflict (union, PR #37 precedent). For the record: randomx-rs = "1.4.1" is a caret req, not the workspace's `=` pin convention (Cargo.lock locks the build); GitHub PR-object lag needed a close/reopen nudge before merge.
- **M9-N2 — DONE (2026-07-23, PR #57, issue #49): WAVE 1 COMPLETE**: P2P core — new `qlab-p2p` crate (boundary-clean: crate + members line + plan doc only). Versioned envelope (12-B header golden-byte-locked; §0 reject-unknown on version AND msg-type, plus trailing/truncated/oversize negatives); **§5 framing NOT re-implemented** — varint + compact-block codec are literal re-exports of `qlab-cbserver`'s golden-locked implementation, `s5_relay_is_byte_identical_to_cbserver` asserts relay bytes == serving wire (single LEB128 in the tree); peer table w/ handshake FSM + scoring/ban (malformed 100 / invalid 20 / welshed-inv 5, ban ≤ −100); inv→getdata gossip w/ bounded seen-cache; header-first sync (exponential locator, 2000/batch); BIP-152-shape block relay w/ missing-tx round-trip; **dual transport** (deterministic in-proc hub + real std::net TCP, same P2pNode logic, TCP loopback integration tests). Stop-point discipline exemplary: docs/m9-n2-p2p-plan.md surfaces the §5-vs-§7 "compact block" terminology split — both implemented, cleanly separated, coordinator ratified keeping both. N1 traits stubbed per plan (`n1.rs` consumer-facing slice + StubNode); real wiring = N7. **430/430 unfiltered workspace on the merged tree, 0 failed; qlab-p2p double-run flake-free** (sonnet T-verify + coordinator diff adjudication). Records: builder omitted the Cargo.lock qlab-p2p entry (coordinator added at merge); UTF-8 decode errors mislabeled as `DecodeError::Varint` (cosmetic). Acceptance side-find: protocol-spec §6's "(106 B" preimage label is arithmetically wrong — fields sum to 98 B (code + goldens are 98) → design-repo correction.
- **M9-N4 — DONE (2026-07-23, PR #58, issue #51)**: mempool + block assembly in qlab-node. `emission.rs` — `coinbase(h) = S_atomic(h+1) − S_atomic(h)` (protocol-spec §6) over the frozen B2 curve, constants pinned directly + S(h) reused from qlab-econ (audit-anchor-identical, no re-derivation); telescoping exactness, tail-activation ~4.5M, 65/15/20 exact-sum (miner absorbs remainder) all as asserting tests. `mempool.rs` — seven-gate admission cheapest-first (posted-fee §4/§5 → finalized in-window anchor §4/§7 → coinbase maturity 144 §2 → consensus double-spend → dup → in-pool nf conflict → injected-verifier proof LAST); free-zone greedy assembly + explicit-selection hard-cap gate (frozen §6 two-median, all six constants test-locked incl. long window 100,000); `txid` field-for-field the body-commitment encoding; coherence integration test both directions (admit ⇔ apply_block accept). All three builder-reported decisions coordinator-RATIFIED: (1) `FEE_MARGINAL_UNITS` 5,000→500,000 — fee absolutes converged to frozen §5 (0.01/0.02/0.04 QMB test-locked; same convergence act as the PR #45 anchor-window fix); (2) node enforces frozen 100k long window, load harness keeps its 5k sim knob; (3) coinbase maturity modeled at the node-policy layer (coinbase-note registry + declared spends) per tokenomics §6 one-hop-transparency — policy + constant pinned, in-circuit maturity is later node-state work. Pre-existing `validate_body` boundary (no cross-check of in-proof fee vs public fee) verified as not-N4-introduced and bound at the injected-real-verifier seam — recorded, no issue filed. **453/453 unfiltered workspace, 0 failed** (sonnet T-verify + coordinator adjudication; run serialized behind N5's builder suite — rig-contention discipline held).
- **M9-N5 — DONE (2026-07-24, PR #59, issue #52)**: committee over network. **Production domain string `b"qumbra:checkpoint:v1"`** (drops `devnet:`; golden-locked incl. a no-devnet-substring assertion; deliberate versioned signature break, §0 discipline; §7 [full-M8] item realized — spec update owed) + cadence 8 = one 10-min anchor bucket (testnet-tunable NOT frozen, annotated) + `is_checkpoint_height`/`next_checkpoint_height` successive-slot scheduling. **Epoch machinery** (`epoch.rs`, replaces the M6 static set): staged membership changes apply ONLY at boundaries (§2), tombstone = immediate quorum exclusion + formal drop at the next boundary, per-epoch sealed history so behind checkpoints verify against the right roster; frozen N=21/quorum 15/epoch 1,152 test-locked. **Downtime jail** = SigningWindow, integer-strict `signed·100 < 33·window` over trailing 100 checkpoint ROUNDS, full-window-only (no false jail on fresh committees), boundary reset. **Equivocation over the network**: new `MsgType::Evidence` (0x0023) + codec (round-trip w/ real ML-DSA sigs, reject-trailing); `observe_votes` records only verified votes (forged votes cannot seed evidence), detects same-slot conflicts, applies the local tombstone BEFORE finalization, gossips evidence; `verify_equivocation` (M6) four-way validation at the slot-epoch committee. Network tests: whole-mesh tombstone convergence (announce + pure auto-detect), tombstoned-votes-quorum-rejection (7 committee, 3 tombstoned, 7 votes → insufficient-quorum), Ebb-and-Flow degrade/recover, epoch progression. **476/476 unfiltered workspace on the merged tree (with N4), 0 failed** (sonnet T-verify + coordinator adjudication; builder had honestly split its runs over a tool timeout — the single-run bar was paid at acceptance). Records: `EQUIVOCATION_SLASH_AMOUNT` still the M6 flat placeholder (frozen §4 = 10 % of bond; bond itself placeholder — params_devnet convergence debt); `evidence_id` is (a,b)-order-sensitive → duplicate relay possible, tombstone idempotent, cosmetic.
- **M9-N6 — DONE (2026-07-24, PR #60, issue #53): WAVE 2 COMPLETE**: node RPC + cbserver embed. Purely additive `qlab-node::rpc` (984 + 198 test lines): `NodeRpc::submit_tx` (live-state checks — anchor-now §4/§7, posted fee §4, spent/in-tx-repeated/pending triple nullifier gate, discovery↔cm binding, injected verifier LAST) + thin pending pool (self-aware N7-rewire placeholder); `/v1/compact` + `/full` = cbserver encoders reused verbatim, **golden digest `3ee2a5e6…f54017` re-asserted through the exact embedded encoder path** + node-stream==cbserver-encoder byte-equality integration test; `/v1/tree/frontier` served from the LIVE depth-32 tree, served frontier reconstructs the node's reported root (protocol-spec §3 exact); new `/v1/status` + `/v1/anchors` wires versioned 0x01 + reject-unknown/trailing (cbserver CodecError vocabulary reused); headline: **unmodified cbserver light client scans a live node over a real socket and finds the planted notes; a stranger finds zero**. **488/488 unfiltered workspace on the N4+N5+N6 merged tree, 0 failed; rpc socket tests double-run flake-free** (sonnet T-verify + coordinator adjudication). Builder's OOM finding ADJUDICATED: "full qlab-bench needs --test-threads=1" NOT reproduced — five green default-parallelism full-suite runs on record (374/430/453/476/488); the real hazard is CONCURRENT suites (two 20–30 GB interior proves from different sessions), which is exactly the coordinator's serialize-T-verify discipline. N7 integration note: `rpc::tx_id` (statement-only, no proof bytes) ≠ `mempool::txid` (body-commitment encoding, proof included) — both deliberate, do NOT conflate when rewiring submit_tx onto the N4 mempool.
- **M9-N7 — DONE (2026-07-24, PR #61, issue #54): 🏁 M9 COMPLETE — the T0 readiness evidence pack is on record.** The three integration mandates delivered: (1) `qlab_p2p::adapter::NodeAdapter<P,V>` implements all five N1 traits over the REAL stack — ChainState + real PoW/LWMA/key-seed validation (N3), MemNode state machine (N1), N4 Mempool w/ injected M3 verifier, N5 EpochCommittee/finality/SigningWindow/equivocation (faithful port of StubNode's verified logic; StubNode retained as regression guard); `ingest_block` validates the BODY before the header (adversarial objects rejected regardless of fork position), tip-only state machine w/ documented reorg-lag tolerance; (2) `rpc::submit_tx` delegates to `Mempool::admit` — the three tx-id encodings kept strictly separate via a `wire_id → mempool_txid` index (the N6 hazard note heeded, "never conflated" documented); (3) `FullNode` composes P2P + node + PoW (KeccakPow soak, RandomXPow smoke proving the N3 slot) + committee-net + RPC views. Soak (four scenarios over an N-node in-process mesh): sync-from-genesis under churn ✓, adversarial peers (5 invalid-object classes all rejected+penalized, no crash) ✓, restart/reorg/partition (genuine fork → heaviest fork-choice → finality never overreached; open==replay) ✓, 1000-round leak check (mempool drains to 0 at every sample) ✓. **Evidence: 503/503 unfiltered workspace, 0 failed; soak double-run reproduced byte-identically INDEPENDENTLY BY THE COORDINATOR (547-line output)** (sonnet T-verify suite + coordinator soak reproduction + diff adjudication). Both flagged changes ratified: defaulted `BlockIngest::ingest_block` (backward-compatible); `BlockAnnounce` + `coinbase: u64` ([devnet-placeholder] §7 wire, not frozen). Recorded finding for M10: announce-flood propagation drops orphan blocks without kicking sync — gap/partition recovery runs through the header-sync path; consider an orphan-triggered sync kick at the internal-net stage. Process notes on record: builder amended a commit after catching an untested change (self-caught); one dispatched subagent returned without executing, builder implemented inline.
- **NEXT: M9 done — M10 (internal multi-node net, testnet-plan §4) awaits Larry's go** (ladder-stage adoption is Larry-personal per operating-model §4). Old queue: issue #24 D3 (leaf F0 digest) when a leaf-digest consumer materializes; M2 device steps [manual]-gated; b4-fallback two-child re-measure @ q43 (quiet rig); params_devnet convergence debt (EQUIVOCATION_SLASH → 10 % of bond when bond converges). Design-side: **🧊 GENESIS FREEZE v1.0 (design PR #35)** remains the binding reference; params_devnet placeholders continue converging to it.
- Out of scope until the docs say otherwise: recursion beyond rung 1 (rung-1 aggregation is now IN scope per qumbra-design `aggregation-rung1.md`), note encryption, networking, consensus.

## Bench discipline (non-negotiable)

1. Pin exact Plonky3-class revs in `Cargo.lock` before measuring anything; bench numbers against a moving prover are meaningless. First M1 task = select and pin.
2. Every recorded result carries: this repo's git rev, prover crate revs, hardware model, OS, power state (AC/battery, thermal).
3. A number is publishable to the design repo only after being reproduced twice on the same rig.
4. Calibrate the rig against published Poseidon2 numbers (benchmark-survey §2) before trusting any conservative-hash cell.
5. **The acceptance bar is the full unfiltered `cargo test --release -p qlab-bench`** — never a mode-scoped filter (`… m4gate`). A filtered run structurally cannot see other modules' cross-checks; exactly that let a stale width pin drift across sessions until PR #23's acceptance caught it.

## Working conventions

- Non-trivial work: `git worktree add ../qumbra-lab-<slug> -b claude/<slug>`, land via PR to `main`, post the full PR URL, clean up the worktree after merge. Trivial single-file fixes may go direct to `main`.
- Layout: `crates/qlab-air` (the fixed-shape AIR), `crates/qlab-bench` (harness). `docs/` is scratch — polished results go to the design repo, not here.
- Rust 2021, workspace-managed. Keep `cargo check` green on every commit.
- Subagents where possible (research, parallel bench runs, doc translation); verify subagent-written files actually exist on disk (`git status` / `wc -l`) before trusting completion reports.
