# qumbra-lab

Prototype lab for **Qumbra**, a post-quantum privacy-chain design exercise. This file orients Claude Code sessions opened in this repo — read it fully before touching code.

## Ownership — read this first

- **This is Larry's personal project.** It is NOT related to Larry's contract work for pqabelian (Abelian/QDay/USD9-swap, which lives under `~/develop/bidot-blochains/`). Do not frame anything here as "propose to Leo/pqabelian"; Larry is the sole decision-maker.
- Repo is **private** (`lai3d/qumbra-lab`). Keep it that way; new sibling repos also default to `--private`.
- All Qumbra repos live under `~/develop/qumbra/` on disk.

## What Qumbra is, in one paragraph

A 2026 greenfield design for a post-quantum privacy L1: note-based UTXO, one global shielded pool (no transparent tier, no rings), one monolithic STARK per transaction (spend authorization inside the proof — no per-spend signatures), conservative hash (Keccak/SHA-class) everywhere in consensus, ML-KEM note encryption, hybrid PoW + small BFT finality committee. Deliberately NOT Abelian's all-lattice PQRingCT route — the design bets that hash/STARK + NIST standard parts beat lattice ring signatures on every engineering axis.

## Canonical design source

The design docs are the **binding spec** for this repo (22 paired EN/ZH + six EN-only appendices; `qumbra-design/README.md` is the index and is authoritative on the count):

- GitHub: [`lai3d/qumbra-design`](https://github.com/qumbra-labs/qumbra-design) (extracted 2026-07 from the bidot-blockchains-design notebook, history preserved)
- Local clone: `~/develop/qumbra/qumbra-design/`

Before touching the AIR, read at minimum: `transaction-model-and-anonymity-set.md` (the circuit's statement) and `performance-budget.md` (targets + the hash-choice framework). `benchmark-survey-2026-07.md` holds the sourced third-party numbers M1 validates against. Docs are EN with `-zh` pairs; EN is authoritative on technical details.

## Design decisions that bind this code

| Decision | Source | Consequence here |
|---|---|---|
| Fixed-shape 2×2-bucket circuit: 2 × depth-32 Merkle membership + 2 × nullifier PRF + 2 × spend-key knowledge + 2 × commitment well-formedness + in-circuit balance ≈ ~90 hash invocations | tx-model doc | `qlab-air` implements exactly this shape; no generality, no zkVM |
| Conservative hash (Keccak/SHA-class) everywhere in consensus; exact pick is **M1's question** | performance-budget §2 | bench matrix = Poseidon2 (calibration baseline only) vs Keccak-f vs SHA-256 vs BLAKE3 raw-AIR |
| Targets: tx ≤ 150 KB, prove ≤ 3 s laptop / ≤ 15 s phone, ~100-bit conjectured security | performance-budget §3–4 | every bench reports against these; the 15 s phone number is the design's riskiest and M2's whole job |
| Raw fixed-shape AIR, no zkVM | performance-budget §4 | the RISC Zero/SP1 7× spread on identical workloads is the zkVM tax we refuse to pay |

If a measurement contradicts a design-doc estimate, the doc gets a correction PR — the design repo has precedent for retracting its own claims (performance-budget §2 retracted tx-model's "layered hedge"). Never silently diverge from the docs.

## Milestones

- **M1–M9 — ALL DONE.** The full per-milestone log (measured numbers, decisions, gotchas, acceptance evidence) moved to [`docs/milestone-log.md`](docs/milestone-log.md) on 2026-07-29 — it is settled history and no longer loads into every session here. **Read it before re-deriving anything about M1–M9**: it carries the frozen consensus config's provenance, the M4 aggregation gate results, and the M9 wave's acceptance records.
- **M10-T0-1 — DONE (2026-07-24, PR #65, issue #62)**: `qumbra-node` binary + genesis tooling. New thin crate (a bin in qlab-node would cycle qlab-p2p → qlab-node — the issue's thin-crate option taken): config (TOML) · genesis (versioned [devnet-placeholder] file baking FROZEN v1.0 + committee₀ 21 ML-DSA keys + genesis block; **hash `4a75b3b8…c2c3` byte-identical across runs, coordinator-reproduced incl. the 21 key files**; wrong-hash node refuses to start) · params_audit (22-row convergence table, docs/m10-t01-params-audit.md; converged rows test-locked) · run (N7 stack over real TCP + RandomXPow + disk persistence + graceful-shutdown snapshot flush; disk-backed `NodeAdapter::open` restart-resumes the header chain from the persisted log). Orphan sync-kick landed in qlab-p2p (orphan announce → header-sync toward announcer, no caching/relaying of orphans, directional test); **EQUIVOCATION_SLASH converged to 10 % of bond** at the adapter (flat placeholder retired, test-locked). Both flagged decisions RATIFIED: (1) params_devnet residuals (BOND_AMOUNT/GENESIS_DIFFICULTY absolute scale) left as recorded debt — genesis file is the frozen source of truth, boundary respected vs T0-2; (2) `DevnetRehearsalVerifier` accepted with a **named M11 entry gate: the real verifier (`qlab_consensus::verify_proof`) becomes the default before any public net** (clearly-labelled, never exercised on a coinbase-only T0, drop-in documented at the same seam). Acceptance side-find → issue #64 amendment: header timestamps advance on a constant 75 s mining clock (honestly annotated) — LWMA sees constant solvetime, so **T0-3 gains precondition item 0: wall-clock timestamps**. **530/530 unfiltered workspace, 0 failed** (sonnet T-verify + coordinator genesis smoke).
- **M10-T0-2 — DONE (2026-07-24, PR #66, issue #63): M10 WAVE 1 COMPLETE.** Finality-stall recovery + telemetry — the Crosslink feature-net lesson, built. `recovery.rs`: persistent `FinalizerState` (slot → signed checkpoint ledger, versioned reject-unknown persistence) + `Finalizer` — **the never-re-sign guard refuses BEFORE producing a signature** (the conflicting vote never exists), idempotent same-checkpoint re-emit, honest persist-before-broadcast window note; `catch_up_slot` (finalize a LATER slot directly, strictly-advancing); `committee_accrual_finalized` = the degraded-mode rule (rewards accrue ONLY on finalized checkpoints — no retroactive manual canonical designation; devnet-grade, tunable, stamped). Adapter: `make_checkpoint_guarded` routes committee voting through the guard (a would-be equivocator contributes no vote). `telemetry.rs`: /v1/telemetry versioned wire (§0 discipline; stall depth + last-finalized age = the two runbook units), net facts injected via `set_net_facts`. Runbook grounded in the four Crosslink pain points (docs/m10-t02-finality-recovery-runbook.md). Tests: stall→degrade→committee-restart→recovery e2e (two-node mesh), never-double-sign + guard-survives-restart-via-persistence, telemetry round-trip/reject-unknown. FROZEN §4 read-only — no STOP-POINT crossed. **539/539 unfiltered workspace on the T0-1+T0-2 merged tree, 0 failed; qlab-p2p double-run flake-free** (sonnet T-verify + coordinator adjudication; builder serialized its own runs behind T0-1's — rig discipline held).
- **M10-T0-3 Phase A — DONE (2026-07-24, PR #67, issue #64 stays open)**: wall-clock timestamps + T0 deploy tooling. `MiningClock` seam on `NodeAdapter` — `Deterministic` default (every in-process sim/test unchanged, N7 soak reproducibility preserved) vs `WallClock` (binary opt-in at run_node; parent-clamped non-decreasing); the flat-vs-moving LWMA-trace contrast test demonstrates the exact defect→fix (constant clock ⇒ trace pinned at genesis difficulty; jittered solvetimes ⇒ non-constant). Deploy: `deploy/deploy.sh` (one build → one genesis + 21 keys → per-node config → rsync/ssh or local-dir), `dry-run.sh`, `qumbra-node check`/preflight, hosts.example, systemd unit, README (RandomX Linux cross-compile caveat). Topology stamps honored: N=4, keys 6/5/5/5, 2+2 partition ⇒ ≤11 keys/side < quorum 15 ⇒ finality correctly stalls during partition. **543/543 unfiltered workspace at default parallelism; dry-run reproduced independently by the coordinator (all four nodes pinned to genesis `4a75b3b8…c2c3`)** (sonnet T-verify + coordinator smoke). Scripts safety-scanned (rm -rf only own temp dirs; rsync scoped; no sudo/curl-pipe).
- **M10-T0-4 — DONE (2026-07-24, PR #69, issue #68)**: real-verifier default + params_devnet absolute-scale convergence. `verifier.rs`: `ConsensusVerifier` (real M3 `qlab_consensus::verify_proof` over frozen CONSENSUS_CFG) is now the **binary's DEFAULT** — closes the named M11 gate early (PR #65 recorded "real verifier default before any public net"); the NO-OP `DevnetRehearsalVerifier` is opt-in behind `--rehearsal-verifier`, logged loudly (⚠️) at startup. The verify path binds proof↔**declared** surface: reconstructs PVs from the tx's declared anchor/nf/cm/fee via `pv_vec`, rejects non-2×2 shapes, verifies against the canonical witness-free AIR — so a proof for a different surface than declared fails (the security-load-bearing property, test-locked: `rejects_a_tampered_public_surface` + `rejects_garbage_and_wrong_shape`). params_devnet: BOND_AMOUNT → genesis bessel scale, GENESIS_DIFFICULTY annotated [devnet-placeholder]; audit doc rows updated. **Build-weight STOP-POINT CLEARED** (not triggered): qumbra-node clean build 17.55 s, binary 2.5 MB — pulling qlab-consensus adds no pathological weight (the plonky3/qlab-air stack was already in the workspace build graph). **547/547 unfiltered workspace, 0 failed; 8 verifier tests (real STARK verify) green** (sonnet T-verify + coordinator diff/security adjudication). The M11 real-verifier-default gate is now closed ahead of schedule.
- **NEXT: M10 T0-3 Phase B — [manual]-gated on Larry's 4 VPSes** (≥4 GB RAM / 2 vCPU / ≥40 GB disk + ssh): genesis rehearsal, four WAN soak scenarios, ≥48 h telemetry run, the T0 evidence pack. Phase B starts from a FRESH branch off main (Phase A was squashed). M11 gates: ~~real-verifier default~~ CLOSED EARLY (T0-4/PR #69); remaining = faucet/explorer/join docs, NAT traversal, halt-height upgrade drill.
- **M10-T0-3 Phase B-lite — DONE (2026-07-25, PR #71, issue #64; docker/localhost rehearsal)**: 4-node docker net over real TCP + RandomX-light + WallClock + 75 s + disk. Scenarios 0/1/2/3/6 PASS — **RandomX-on-Linux-aarch64 build RESOLVED (reusable VPS image)**, in-container genesis hash == pinned `4a75b3b8…c2c3`, late-joiner header-sync, mining-node restart open==replay on disk, **6.26 h steady soak (77 samples, tip 388, 4 nodes lockstep ≤2-block spread, no fork, ~262 MiB/node, zero misbehavior)**. run.rs = additive telemetry only (coordinator-read); docker/ tooling + soak.sh + honest run doc landed. **548/548 unfiltered workspace on the merged tree (independent coordinator run — NOT the builder's filtered 91+28), 0 failed. 🔴 HEADLINE FINDING [#70](https://github.com/qumbra-labs/qumbra-lab/issues/70): distributed committee finality never forms** (21 keys 6/5/5/5, no node ≥ quorum 15, no cross-node vote-aggregation protocol) — **liveness gap NOT safety** (Degraded/Ebb-and-Flow as designed, nothing wrongly finalized, NOT a STOP-POINT); exactly the gap a T0 net exists to catch, invisible to N7's concentrated-key in-process soak. §4/§5 partition + committee-stall scenarios deferred to the #70 fix. Minor: 🟡 dial-once/no-re-dial (partition-heal needs restart) folds into #70-era; 🟢 multi-miner LWMA ramp = correct localhost artifact. WAN-only items (real RTT, LWMA at WAN pacing, ≥48 h) still owed to Phase B-WAN.
- **M10-T0-5 — DONE (2026-07-25, PR #72, issue #70 CLOSED): 🏁 the M10 software half is complete.** Cross-node checkpoint-vote aggregation — the fix for the Phase B-lite headline finding. New `qlab-devnet::tally::VoteTally`: a **bounded** per-checkpoint-variant accumulator that de-duplicates verified, active votes by signer index across *any number* of messages, with three named caps (`MAX_TALLIED_SLOTS=8`, `MAX_VARIANTS_PER_SLOT=4`, `TALLY_TIP_SLACK=8`=cadence; window `(finalized, tip+slack]`) — devnet-grade, testnet-tunable, NOT frozen; deliberately **not persisted** (rebuilds from re-gossip). Wire: `MsgType::CheckpointVotes = 0x0024`, body byte-identical to `Checkpoint` 0x0022 (codec NOT forked; golden header + 73-B empty-body vectors locked). Relay is **direct push gated on "the local tally grew"** — which both terminates relay and dodges blocker (3) entirely; `InvKind = 4` reserved but unused (coordinator-ratified deviation from stamp S3 — bypassing the inv path makes the dedup collision *unreachable* rather than disambiguated). Scoring fixed (S5): a well-formed below-quorum set is `Learned`, relayed, **never penalised**; only forged / unknown-signer / duplicate sets are `Invalid`. Binary wired at last to T0-2's never-double-sign `Finalizer` with **sign → fsync → broadcast** ordering (S7), plus periodic re-dial (S9) and honest `age_s=0` when nothing is finalized (S8). **The safety property, and the check that decided the merge: `finality.rs` and `committee.rs` have an EMPTY diff** — `try_finalize`, `quorum_threshold` ((2n)/3+1), `verify_vote` and the preimage `b"qumbra:checkpoint:v1"` are byte-identical. Two independent gates: every vote is roster-/dup-/signature-checked before it may enter the tally, and the unchanged `try_finalize` re-verifies the **whole accumulated set** at finalize time, so a poisoned tally still cannot finalize. Tombstone handling is stricter than before — filtered on insert *and* re-filtered against current status at finalization. **569/569 unfiltered workspace, 0 failed** (548 baseline + 21 new; independent sonnet T-verify in a detached review worktree + coordinator diff adjudication). Docker evidence: 6/5/5/5 with **no node holding quorum 15** finalizes on the cadence grid (`final` 32 → 232 monotone over a 135-min soak, ≤1-block spread, `regime=Final` 107/108); §4 2+2 partition correctly stalls both sides (11 and 10 keys) and heals **with no restart**; §5 committee stall → recovery re-forms and catches finality up across the stalled span (demonstrating the S7 rebuild-from-re-gossip property for real). No STOP-POINT crossed. Mid-baton coordinator ruling **S5a**: the `qlab-bench` n7soak case asserting that sub-quorum sets are penalised was the *fossil of the bug*, so the §5 zero-contact list was amended for that crate to test-semantics-only, on the binding condition that adversarial coverage not fall (case (d) keeps its not-finalized assertion, new forged (d2) carries the penalty). **Recorded follow-ups (non-blocking):** (1) `docs/m10-t02-finality-recovery-runbook.md` §2 — `stall_depth` and `last_finalized_age_secs` are no longer the same stall in two units at cold start (caveat added in the same pass); (2) `adapter.rs`'s `Err(_) => Invalid` catch-all would penalise an honest peer for a broken internal invariant — unreachable today, prefer a log; (3) out-of-window heights are `Stale` and unrelayed, so a far-behind node won't propagate ahead-of-tip votes (self-heals; watch at Phase B-WAN).
- **M10 Phase B-WAN — SOAK SEALED 2026-07-28 (started 2026-07-26 15:43); the four drills are still owed.** The `[manual]` gate is discharged: 4 × AWS `t4g.small` (Graviton/arm64, Debian 12) across **three continents** — us-east-1 · eu-west-1 · ap-southeast-1 · ap-northeast-1 — provisioned by Terraform from the new private [`qumbra-deploy`](https://github.com/qumbra-labs/qumbra-deploy) repo, digest-pinned image from GHCR (no registry credential on any host), genesis + the 21 keys split 6/5/5/5 by `deploy/deploy.sh`. **Measured RTT baseline 68–223 ms**, taken before the net started. Distributed finality forms and advances across real intercontinental latency with **no node holding a quorum** — #70's property on a real WAN. **Bar met: 48.01 h minimum uptime across the four, zero restarts, zero finality reversions, all four ending at the same `final`** — evidence pack `docs/m10-t03-phase-b-wan-run.md` + `-zh` + the raw sampler log (PR #93). The 1,152-block committee epoch boundary was crossed at real pacing **twice**: 0→1 at `tip=1152` with all four within 28 s, 1→2 at `tip=2304` within 79 s. LWMA at WAN pacing measured over 1,707 intervals (mean 86 s / median 60 s / p99 320 s vs a 75 s target) — ordinary PoW variance. Two operator-path defects were paid for on the way: `deploy.sh`'s `--delete-excluded` breaks macOS-openrsync → GNU-rsync (receiver dies, payload undelivered, fixed to `--delete`), and Terraform inline SG rules silently deleted the peer P2P rules on a later apply (green apply, healthy instances, every dial timing out). Operations now run as **T-ops**, a fourth tier (`operating-model.md`); playbook + live state in `qumbra-deploy/OPERATOR.md`.
- **M11 halt-height upgrade mechanism + drill — DONE (2026-07-27, PR #76, issue #74)**: pulled forward from M11. Four drills; the halt marker records the frozen *digest* **as an audit field, and the gate keys on the halt HEIGHT** (`release.rs` `check_against_marker`). ⚠️ This line previously claimed the gate keyed on digest equality; it did not — that was #81's *decision*, recorded here before it was built, and a reader would have concluded #81 was already done. Corrected 2026-07-31 in the #81 baton. §4 measured update landed design-side (`a002965`).
- **M11 halt marker records the revision IN FORCE — DONE (2026-07-31, issue #81)**: the gate's real question is "is this binary's parameter set the one this chain is running", not "which height did this node halt at". Height was a proxy good for exactly one upgrade, so after halting at 16 *every* later release had to declare `resumes_from = 16` forever. Now: marker schema 1 records the in-force revision and whether the boundary was **passed**, rewritten by the binary that legitimately resumes; the gate keys on **`Revision::digest()` equality** once passed, and keeps the declaration while still halted *at* the boundary (there the pre-halt revision is trivially in force, so digest equality alone would re-admit the pre-announcement binary — the one refusal the marker exists for). **Not the bare frozen digest**: the drill's upgrade is inert, so `v1.0` and `v1.0.1-drill` share a frozen digest and only the identifier-bound digest still refuses a downgrade. Two corrections to the decision as filed, both reported on #81: (1) 🔴 digest equality **alone forks the chain** — `PostHaltRules.domain` is mixed into the PoW value and came only from the binary's own `resumes_from`, so the routine release this unblocks would have started with no domain and rejected every post-boundary block; the domain is now read from the marker (`rule_schedule_on`), folded in as a precondition; (2) a pre-#81 (schema 0) marker is **refused with a reason**, never migrated by guesswork and never a fall-through to a fresh start. Also fixed a latent defect the second upgrade exposed: `marker_final_written` was seeded from *any* marker's `boundary_finalized`, so a second halt on a resumed data dir never wrote its marker — unreachable before #81 because the height gate refused such a binary outright. **🔴 STILL OPEN (reported, not built):** one marker cannot express domain *history*, so after two upgrades a node syncing **from genesis** would compute the first upgrade's span under the second revision's domain. Pre-existing (a release carries one `resumes_from`), invisible on the snapshot-resume path, and the fix is the unbounded "releases carry boundary history" alternative #81 rejected — needs a coordinator decision.
- **i77 header↔body binding — DONE (2026-07-27, PR #79, issue #77)**: block bodies are bound to their header — a consensus-correctness fix. Carries the `is_peer_fault()` conversion (`Rejected` is a peer fault, `Ignored` — a block above halt height — is not, because a peer still mining is on a different release, which `committee-and-governance` §4 permits).
- **Issue #24 D3 — DONE (2026-07-27, PR #82): issue #24 CLOSED.** The leaf F0 digest input binding. F0 is the challenger's first observation flush and its message *is* the claimed public surface, yet nothing constrained it: a leaf could claim any transaction surface regardless of the proof it verified (issue #21's R2), and the Fiat–Shamir transcript started from a prover-chosen message. D3 binds every F0 rate word via the resurrected `shape_mosaic`/`WordBind` table and adds the `f0dig` public-surface digest. **Zero footprint cost**: interior b2/q86 peak 19.75 / 20.42 GB builder + 19.75 GB coordinator-independent (rev `4ae6936`, 7 MB spread), all below the same-session pre-D3 baseline of 20.66 GB, 36–38 % margin against the 32 GB envelope. **626 passed / 0 failed** unfiltered at merge. Coordinator-required end-to-end run passed: whole two-level tree 18.58 s / 21.16 GB peak, full native verification chain, both #24 consumer invariants recomputing over the f0dig-extended opvs. Design-side measured update landed (`95a1d69`) **with a dated correction: the leaf is 839.2 KB at q43 and the interior root 1.65 MB at q86 — the 779.6 KB / 1.51 MB figures elsewhere are q40/q80-era and must not be differenced.**
- **M11 peer discovery — DONE (2026-07-28, PR #86, issue #83): the first M11 baton.** Nodes learn addresses, judge which are actually dialable, auto-connect within caps, gossip only dialable addresses, and carry what they learned across restart. Four pinned decisions: **dialable = we successfully connected out to it** (not self-reported, not passively observed — on a net that accepts outbound-only participants, "it connected to us" proves nothing), sticky across disconnect because a partition is not proof of unreachability; **self-reachability via explicit `advertise_addr`** (a node never sees its own public address; changing that would be a payload change = stop-point); **caps 8/32/1024/100** with the inbound cap enforced *at accept* before registration; **`peers.dat` persistence** (only dialable entries, versioned, rejects unknown) — without it every restart collapses to "only seeds are dialable", which is exactly the state the NAT trigger watches for. S9's re-dial moved from `qumbra-node` into `qlab-p2p::addrman` so seeds and learned addresses share one path, one cap, one backoff ladder. `PeerTable::addr_book`/`remember_addr` removed — it recorded every handshaked peer, i.e. the "merely connected" set S2 forbids serving, and keeping it would have been a second ledger. **668 passed / 0 failed** unfiltered.
- **M11 peer hardening — DONE (2026-07-29, PR #99, issue #91)**: inbound rate limiting, resource bounds, eclipse-resistant peer diversity. Rate limiting sits **ahead of decode and dispatch** in `tick` and a throttled frame is dropped and **not scored** — volume is not a protocol fault. *(Now also the leading suspect in #107; see below.)*
- **M11 committee diagnostics + `/metrics` — DONE (2026-07-29, PR #96, issue #87)**: round-level `ROUND` journal lines (`why` / `have` / `need` / `voted` / `absent` / `variants` / `quorum_ms`) plus a Prometheus surface that is **off unless `metrics_addr` is set**. This is what made every #106 and #107 finding below legible; before it, a failed round left no trace.
- **M11 faucet — DONE (2026-07-29, PR #103, issue #100)**: the lab's 15th crate, `qlab-faucet` — a proof-generating wallet plus an off-chain request gate. **762 passed / 0 failed** unfiltered at acceptance. Two findings came out of it and both are open ([#101](https://github.com/qumbra-labs/qumbra-lab/issues/101), [#102](https://github.com/qumbra-labs/qumbra-lab/issues/102)); see the T1 note under NEXT.
- **#84 finalized-checkpoint identity — DONE (2026-07-29, PR #110)**: `fid` / `sslot` / `sid` on `TELEMETRY`, `cpid` in `ROUND`, three **unlabelled** gauges on `/metrics` (a hash as a Prometheus label grows 144 series per node per day, forever). **780 passed / 0 failed** unfiltered. Until this landed, two nodes that finalized *different* checkpoints at the same height printed identical telemetry — `final=` says how high, never *what*, so **the most severe consensus failure this net can have was invisible to the only instrument an operator has.** `deploy/docker/soak.sh`'s halt-drill stop-check now tests that condition directly instead of by proxy (PR #111), and `qumbra-deploy/OPERATOR.md` §3 carries the cross-host check — with the two divergences kept apart, because a `fid` split is a STOP and a `sid` split is a finding.

- **M11 faucet listener — DONE (2026-07-30, PR #128, issue #123)**: `qumbra-faucet`, the lab's **17th crate** — an HTTP surface a browser can use over `qlab-faucet`'s tested `accept` → `dispense` core, running **in-process with a keyless node** (§6.2 decision 1, enforced as a startup refusal: a node config naming any committee key file does not start). No `POST /v1/tx`, no new node route, no wire change — the only consensus write is `P2pNode::announce_tx` via three additive `qumbra-node` seams (`run_until_with`, `state`, `submit_local_tx`). Three positions taken: **own crate** (Cargo has no per-target deps, so a bin in `qlab-faucet` would put the whole node graph into the *library's* dependencies — the same call #117 made for `qumbra-opview`); **an unservable faucet refuses with a height rather than queueing**, before the gate so no ticket is burned; **the page shows two chain integers and no more** (tip + finalized; no per-signer participation ever, and `qumbra-opview` deliberately not mounted on the one host holding a hot key). The frozen §2 maturity gate is honoured at the **funding boundary** at exactly `Mempool::admit`'s prospective-height threshold (test-locked against the mempool itself). **36/36 targeted (`-p qumbra-faucet`), 0 failed; one real 2×2 grant proof 2.11 s, peak RSS 11.89 GB** (`/usr/bin/time -l`, release, `--test-threads=1`, 1 sample). 🔴 **HEADLINE FINDING**: a grant's recipient **cannot detect or spend it** on a `qumbra-node` net — see the corrected T1 bullet below. Also found: `NodeAdapter::ingest_tx` hardcodes an empty coinbase declaration, so #102's unreachable-maturity-gate finding is **one seam wider** than #102 records (the wire, not just the wallet RPC).
- **#117 fid on the wire + `qumbra-opview` — DONE (2026-07-30, PR #119, issue #117)**: `fid` on `/v1/telemetry` (`RPC_VERSION 0x02` — a wire-version bump) and **`qumbra-opview`**, the lab's **16th crate** — the T0 operator view for *do these N nodes agree on what they finalized*. Deliberately **not** `testnet-plan` §6's T1 minimal explorer (PR body states this). Without `fid`, every T0 drill's pass/fail criterion was inexpressible.
- **#120 main_chain_counts from applied state — DONE (2026-07-30, PR #120)**: derive the counts from applied state rather than a side ledger.
- **#105 rfail counts live rounds — DONE (2026-07-30, PR #122, issue #105)**: `rfail` counts live rounds, not the history a node walked through.
- **#73 age_s at genesis head — DONE (2026-07-30, PR #125, issue #73)**: `age_s` refuses to state an age while the finalized head is genesis.
- **#121 committee + exact supply on opview — DONE (2026-07-30, PR #126, issue #121)**: the last quarter of the chain-health surface — committee status and exact supply attestation on `qumbra-opview`. Coordinator-independent acceptance **887** unfiltered (spine: 887 → 890 → 902 → 911 → 913 → 915 through #141).
- **#127 self-filter from local identity — DONE (2026-07-30, PR #131, issue #127)**: self-filter fires when `advertise_addr` is unset, not only when it is set to self.
- **#107 outbound connects off the node loop — DONE (2026-07-30, PR #132, issue #107)**: move outbound TCP connects off the node loop. Coordinator-independent acceptance **890** unfiltered. (The dial-duration evidence that #132 discarded landed as #137 / PR #139.)
- **#130 (a) state machine catch-up — DONE (2026-07-30, PR #142, issue #130)**: let the state machine catch up with its own chain — apply a body for a header we hold, ascending, and refuse the duties a stale view cannot answer. Coordinator-independent acceptance **902** unfiltered. The (b) half (joiner bans for correct history) remains open as a separate baton.
- **#102 coinbase maturity structural — DONE (2026-07-30, PR #140, issue #102)**: enforce coinbase maturity structurally, not by policy — a **pre-mint consensus change**. Coordinator-independent acceptance **911** unfiltered.
- **#137 dial duration on completion — DONE (2026-07-31, PR #139, issue #137)**: log the dial duration on completion (the evidence #132 discarded). Coordinator-independent acceptance **913** unfiltered.
- **#136 opview exit code / UNAVAILABLE — DONE (2026-07-31, PR #141, issue #136)**: opview exit 0 attests *no detected divergence*, not supply coverage; pin the `UNAVAILABLE` token. Coordinator-independent acceptance **915** unfiltered.
- **#106 item (1) mine-before-sync — DONE (2026-07-31, PR #153, issue #106 STAYS OPEN)**: a node cold-started on an empty data dir no longer mines its own chain from genesis. **Two defects, either one sufficient**: the mining gate had no sync condition, *and* `SyncPhase::Idle` meant both "caught up" and "no taller peer known yet" while `maybe_start_sync` flattened `best_height()`'s `None` to 0 — so a node with an empty peer table **asserted `Synced` at height 0 on zero evidence**, and a gate reading `SyncPhase` would have mined anyway (`on_headers` carried the same conflation; `PeerTable::best_height` now ignores a `Ready` peer that never sent a `Version`, whose `tip_height: 0` default was a fabricated claim of genesis). #130 (a)'s `state_lag` gate does **not** cover any of this: it compares the node's own two views, both 0 on a cold start. The rule taken, derived from `dial_peers` rather than a new flag or a wall-clock grace period: **before its first block a node must either know where the chain is, or know there is no chain to know about** — no address in the book *and* no live peer means it is the net, which is how a brand-new net still starts; the verdict **latches** on first clearance, so it is a startup gate and not a standing check a lying peer could weaponise. `mready=` appended to `TELEMETRY` (`PRE_I84_FIELDS` untouched) because **a node deliberately not mining looked exactly like a node failing to mine** — the Terraform/SG failure of Phase B-WAN would have read `mready=unknown` on all four hosts. **Tip freshness (Bitcoin's IBD shape) is unusable and recorded so nobody rebuilds it: genesis is stamped `timestamp = 0` for a reproducible genesis hash**, so a cold node's tip has no wall-clock anchor at the one height where the answer matters. **921 passed / 0 failed** unfiltered (915 + 6), mutation-checked in both directions (gate disabled ⇒ the fork tests fail; `Alone` dropped ⇒ the genuinely-first-node test fails). Two findings recorded, neither fixed: **historical bodies are never served** (`GetData(InvKind::Block)` answers with a header only, so a rolled host now sits at `mready=synced slag=N` — #104/#130 territory, *not* this gate misfiring), and **committee signing is coupled to mining success** (`try_checkpoint` runs only inside `if self.try_mine()`, so a `mining = false` node holding keys never proposes a checkpoint). `recovery.rs`/`finality.rs` empty diff — the never-double-sign guard, which this incident challenged three times and which held, was not touched.

- **#212 durable head on the wire — DONE (2026-08-03, PR #242)**: `dfin`/`dfinbh` on `/v1/telemetry` (`RPC_VERSION 0x03 → 0x04`) — head #3, the only finalized head that survives a restart, finally visible to the drills' instrument. `BlockIdentity` newtype (no `PartialEq<u64>`) so a block-hash prefix cannot compile against a checkpoint identity; the roll problem solved reader-side (compat over a named version list `[0x03, 0x04]`, node strictness unchanged — `>=` proven unworkable on a non-skippable tail). Coordinator rulings on the PR: (a) accepted, (b) declined-with-trigger, recompute-identity (c) **VETOED** (an asserted input is not a recorded fact; 棒 5's real anchor root would turn it into a false 🔴 STOP on every healthy node), severities as built (cross-host durable split = 🔴 exit 2; single-node head #1 ≠ head #3 = 🟡 token). Follow-up hazard filed [#245](https://github.com/qumbra-labs/qumbra-lab/issues/245) (`fid`/`sid` still bare `u64`). **1153/0 unfiltered, reconciled** (+19 new − 3 renamed `…at_0x03` tests).
- **#219 ROLE_BNF2 latch — DONE (2026-08-03, PR #239), feature `q69-latch`, default OFF**: 3 cols / 7 constraints / +348 B (**145,957 B featured, byte-exact**; unfeatured wire byte-identical, test-locked). The load-bearing measurement: **quotient degree does not move** — `main`'s max constraint degree was already 4 (the 13 materialized role selectors), `IsTransition` is degree-free, so the real budget is deg ≤ 5 and "stay ≤ 3" was never the cliff. 2-col variant (+232 B) **declined-with-trigger** (grounds on PR #239: 116 B vs degree headroom in the exact seam 棒 5 edits next, and the pre-mint combination tree must measure what was ruled). **Mint sequencing ruled on #219**: one combination tree (#215 (i) + #188 (a) + latch-on) measured byte-exact before `CONSENSUS_WIRE_BYTES` moves — 148,161 B is a sum, not a mintable number; latch default-on is decided at that baton's ratification. Enabling the feature = genesis change, stop-point held.
- **#235 qumbra-explorer — DONE (2026-08-03, PR #236)**: the public chain-health page from its own **keyless** observer node — the 18th crate, deliberately not an Etherscan; non-loopback `discovery_addr` refused by name (review fix `fba4d04`, +1 test). Verified 1150/0 solo on a pre-#242 tree, then **re-verified in the combined tree against #242's 0x04 wire** — the same-seam composition was the thing needing proof, and the solo green was deliberately not reused as evidence.
- **#243 qumbra-wallet — DONE (2026-08-04, PR #244)**: the end-user wallet CLI — the 19th crate: keygen / restore / address / backup / scan, **deliberately no `send`** (C3-gated on #219 + the mint; the seam is named in `lib.rs`). Positions: indices-not-diversifiers cursor (loss costs re-derivation, never funds), stdin-only restore (argv is `ps`-visible), BIP-39 refused by name through the real process, `UNAVAILABLE` token shared with opview/explorer (one grep covers three surfaces), no partial totals ever; seed-version gate hardened per the #246 finding (+1 test). **Batch acceptance on the combined tree: 1186/0/1 unfiltered, reconciled to the function** — record and merge mechanics on PR #244.

- **#246 rung A qumbra-ffi — DONE (2026-08-04, PR #247)**: the wallet kernel over a hand C ABI (20th crate), cross-compiled `aarch64-apple-ios`/`-sim`; header pinned to source by a bidirectional test; one key-material return behind the CLI's `--reveal` discipline; platform sources all entropy. Enabling change: `qlab-cbserver` gains a `devnet` feature (default ON, existing consumers unchanged) so iOS skips the RandomX C++ chain. Its finding — a **vacuous mnemonic version gate** (round-trip decodes at current `SEED_VERSION` regardless of the stored byte; a wrong byte silently derives a *different wallet*) — was absorbed into #244 pre-merge. **1192/0 unfiltered** at acceptance. Rung B = SwiftUI shell in `qumbra-labs/qumbra-wallet-ios`; ladder on #246.
- **🧊 THE MINT — DONE (2026-08-04, PR #252, squash `df09d08`; the commit title's "DO NOT MERGE — stages 0-2" is stale — all five stages landed and the merge WAS the ratification, Larry's word 2026-08-04 20:26 +08).** One pre-T1 re-mint, three riders, task book `docs/prompts/mint-combo-builder-prompt.md` (corrected three times mid-baton, each with a dated block): **(1) #215 (i)/option 4, the one-permutation form** — `ρ′_0 = nf_0`, `ρ′_1 = H(nf_0 ‖ D_P|1)` via `ROLE_ARHO` (83 → 84 perms, code 14 spent) + a third equality bank (23 columns, `SE_RHO` the one the stage-0 estimate missed) — faerie gold closed structurally; forgery-shape tamper tests (republish the matching commitment so only the bank can refuse) are the new house standard. **(2) The #219 latch unconditional** (both feature gates deleted; single-note spends legal; `dv` slot-0 invariant now doubly load-bearing — it is option 4's soundness anchor). **(3) #188 (a) as amended** — the 120 B full-plaintext payload (`Ivk` cannot derive `rkm` by #32's design; the served wire carries no nullifier) relocated into the **committed** body region; `/v1/compact` serves by projection (`committed_contents_prefix`), verified by the two-goldens-opposite-directions check (serving vector must not move — didn't; body commitment must move — did, +2×120 B); `SealedSeedMismatch` attributes and does not refuse (test-locked). **Numbers**: width 617 → 643 (column-accounted), degree/chunks unmoved at 4, `CONSENSUS_WIRE_BYTES` 145,609 → **148,625 B** (5/5 byte-exact), genesis **`138e1524…addb`** (format v4) — reproduced byte-identical twice by builder AND coordinator through the real CLI; consensus prove 12.21 GB / 3.06 s [1 sample, labelled]. **Found on the way**: the M4 gate's `TW=617` literal (now derived — the aggregation lane had been verifying a shape the prover no longer emits), the faucet's release-compiled-out `debug_assert` on the cm seam (promoted; sweep #253), the recipient-cannot-spend loop (fixed by the derivation itself), and the **b4-fallback envelope breach at 33.51 GB** (attribution pending, #257 — b2 default lane CLEAR at 35 % margin). **Acceptance: 1218/0/1 unfiltered, coordinator-independent at `85a2eb5`.** Next: T0 re-mint/redeploy (T-ops, OPERATOR.md §4), then the four drills.

- **#299 (a) audit-emission — DONE (2026-08-09, PR #301, Multica QUM-82)**: read-only localization tool, `qumbra-node audit-emission --data-dir [--from --to]` — reconstructs the main chain through the real restart path (`Node::open` + `main_chain_of`, no second log reader), compares every committed `body.coinbase` to `emission::coinbase(h)` (imported, never copied), exit codes 0/1/2 test-locked, genesis explicitly skipped (`coinbase(0)=5×10⁹` vs committed 0). **1313/0 unfiltered at acceptance (= 1299 + 14, reconciled).** The fleet run it enabled settled #299's §5–§6 in one pass: **exactly one defective block — height 1377, committed `coinbase(1378)`, −4114 bessel — identical across all four fleet data dirs AND svc0's explorer copy.** The other 24 report lines were 12 cancelling ±1 pairs = **[#303](https://github.com/qumbra-labs/qumbra-lab/issues/303): `s_atomic` is platform-dependent** (glibc vs Apple libm, last-ulp `exp` amplified ~10¹² by the `1−exp(·)` cancellation; measured directly, e.g. `s_atomic(260)` differs by 1) — a hard prerequisite for #299's activation-gated validity rule (protocol-spec §6 amendment, design PR #128), since an f64-evaluated rule forks a mixed-platform net. Census baton (QUM-83) dispatched.
- **🏁 THE EMISSION BOUNDARY IS PASSED — 2026-08-12 (issues #299 + #303 CLOSED; the last chain-side gate before T1 public mining is discharged).** The 8,640 boundary halted at 08:20:55 (four hosts, pre-boundary fid unanimous) and then **could not finalize itself** — the halt window became a live triple-defect repair: **#360** `try_checkpoint` was reachable only inside `if self.try_mine()`, so a halted node never proposes or signs the boundary slot (fix PR #361: `maintain_boundary_checkpoint` on the loop cadence; 1,534/0/7); a **frozen same-height tip tie** (two miners raced 8,640 — variants split 10/5, resolved not deadlocked because node3's pre-halt votes were already on the winner); **#362** the growth-gated vote relay leaves per-process islands after staggered restarts — 16 of 21 keys' signatures existed with no tally holding 15, and each restart's one push fires before peers reconnect (fix PR #363: `VoteTally::variants_at` + `P2pNode::repush_slot_votes` + a 60 s `REPUSH` cadence while halted-unfinalized; 1,535/0/7). node1 returned with both fixes 11:51:40 and the cascade finalized the boundary in <4 min: **11:55:42, fid `7925d1de` unanimous 4-of-4**. Resume waves 11:58–12:02 (`a0ec3c03` = v1.1-exact-emission + both fixes); **chain resumed 12:16:51**; first exact-rule checkpoint 8,648; **§7 `audit-emission --from 8641`: 0 mismatches, exit 0, ×3 hosts**. Grandfathering held (1377 scar intact, pins live). Open: node3's rejoin (gated on #359 — snapshotless genesis replay + serial body fetch below block rate), and the drill follow-up: run a committee TO a halt — today's three defects were all structurally invisible to the offline drills. Suite spine **1,535/0/7**. Records: the #299 thread (STOP → passed → §7), deploy `tasks/boundary-day-8640-2026-08-12.md`.
- **🧊 THE EMISSION GATE — the T1 consensus door (2026-08-10, PR #325, lab #299 + #303).** Two defects, one boundary: consensus never validated `body.coinbase` against the schedule (#299 — one under-paying block, height 1377, −4114, fleet-unanimous, localized by the #301 audit tool) AND the schedule itself was platform-dependent (#303 — glibc's f64 `s_atomic` diverges from exact math at 99.8 % of 4.6 M heights; census: an f64-evaluated rule forks a mixed-platform net). Fix, both arriving at `RULE_BOUNDARY_HEIGHT = 18_000`: the canonical schedule becomes integer/fixed-point `emission_exact` (128-bit square-and-multiply, no libm reachable from consensus), and `validate_body` enforces `body.coinbase == coinbase_exact(height)` above the boundary. Grandfathering is a **property** (`height > boundary`, structural genesis exemption, locked at five candidate boundaries); the 1377 scar is annotated `KNOWN_SCAR` (opview/explorer stop escalating, still publish −4114). The `SupplyLedger` reorg gap closed same PR. **The math-exact proof, reproduced three independent ways** — QUM-83's Python `decimal` 80/120-digit, the builder's Rust fixed-point, and the coordinator's own rig run — all hash the `0..=4.6M` stream to `2e26f6ff…d544a69` with `near_ties == [2]` (h=2 the only tie, proven by divisibility; the census's 9,007 UNRESOLVED all resolved by a `2^-49` guard). **Acceptance 1410/0/6, reconciled** (1365 + 45 run + 1 ignored). Merge is a no-op on the current fleet (~6 k, boundary 18 k, ~10-day runway, pins unset ⇒ historical walk). House-rule refined and recorded (`rpc.rs`, PR #317): existing-bytes change bumps `RPC_VERSION`, pure route addition does not. **🔴 Activation owed (T-ops, ~10 days): `qumbra-node emission-pins` on a glibc host → set 3 pins → deploy the ARMED binary before height 18 k** (`docs/i299-emission-boundary-activation.md`). #299/#303 stay open until the boundary is active. **This is the last chain-side gate before T1 public mining.**
  - **🧊 ARMED ON THE LIVE FLEET — 2026-08-10 (T-ops, coordinator-executed).** The activation's coordinator-owned step-0 landed first: **the three glibc pins** (PR #336) — `qumbra-node emission-pins` in a linux/aarch64 container (header verified `linux`, the one mistake the step can make), coordinator-verified independently (`s_atomic(18001)=89341051168064`, epochs 0/1/2 vs #299's body, epoch 3 an independent eval); the two `unpinned at merge` guards flipped to golden assertions; **1491/0/7, +1 reconciled** (main had moved under it — the parallel session's #327 scan-hoist landed, accounting for two `scan::tests` in the delta). Then a **Dockerfile `NODE_FEATURES` build-arg** (PR #337) to cut both variants from one recipe. Both images built + banner-verified via `halt-status`: **armed `06baa298…4338`** (v1.0, `ARMED — halts at height 18000`) and **resume `08b6c306…8385`** (v1.1-exact-emission, `resumes past 18000`), resume **pushed first** per the doc. Fleet rolled 931afa0f → 06baa298 (deploy PR #113/#114), node1→2→3→0, 13:40–16:06 +08: node1/2 clean; **node3's #300 spin did not recur** (~90 s replay); **node0 hit a 2,700-record ~90-min replay** that read as *replay, not hang*, **because of #287's new progress line** (`39%…climbing`) — the fix's first real operational value, hours after it merged; the **R2 guard (same-height fid split / finality regression) never fired** across the whole window while the net held on 15 keys = exactly quorum. All four ended `rev=b2fce07`, `final=6720`, one `fid`; verified healthy 4 h later (`final=6912`, uniform). **🔴 What remains, ~2026-08-20 at ~48 blk/h**: the net halts at 18000; roll the resume image to **all SIX hosts** (svc0/svc1 predate the emission gate and must join the resume roll — OPERATOR §3, deploy PR #115), then the coordinator adjudicates the boundary `fid` (a split there is R2) and finality resumes above 18000 under the exact schedule. Sequence: `docs/i299-emission-boundary-activation.md` §5–7.
- **🧊 NAME SERVICE CONSTRUCTION — MERGED INERT (2026-08-12, PR #381, issue #367 stays open as the arming tracker): the T2 rider, built ahead of its gate on Larry's #367 ruling.** `NAME_RULE_BOUNDARY_HEIGHT = None` (pins-unset precedent) ⇒ zero behavior change on the running chain; activation = a future halt boundary Larry stamps at T2 arming. What landed: `qlab_devnet::names` (fee table 1/32/128/512/2048 QMB ratified at merge, N3 grammar, rider codec, domain-separated commit hash) · height-keyed body commitment (v2 byte-exact at/below boundary — the old golden is the live-chain compat lock; `qumbra:body:v3` above) · presence-conditional tx-wire rider (rider-free tx byte-identical to the old wire, no relay partition) · additive `WireRecord` log variant (**FORMAT_VERSION stays 3, zero datadir migration**) · `check_op` rules + `validate_body_above` drill seam + fee split (posted + burned name fee) · `NameRegistry` on the `apply_state` funnel + `names.bin` sidecar (applied_height-checked, unusable ⇒ full-replay fall-through) · the burn (`coinbase_note_value` subtracts `total_name_burn`; SupplyLedger `burned` column; **telemetry wire deliberately unbumped** — RPC_VERSION bump owed at arming) · `audit-names` (exit 0/1/2, re-derive + reconcile vs the restored registry) · `/v1/names` on both servers (D2's resolve-by-name refused BY NAME, 400) · `qumbra_wallet::names` (registry cache, pins/known_hosts with the REBIND alarm, #312-guarded sync, `send --to NAME.qmb` behind the pin gate, resumable register with salt-before-broadcast) · four adversarial drills (front-run race — min-age window is the mutation check; expiry-snipe e2e ends at the alarm; reorg re-fold no-residue; 2,000-round canonicity fuzz). Found by its own drill pre-ship: the pins layer's constant undecodable-address marker made two different bad addresses compare `Match` — now bytes-derived. **Acceptance: suite-arm64 run 31576745820, 1,585/0/7 reconciled (1,535 + exactly 50 new), negatives zero, 16.3 GB peak — the first acceptance taken entirely off the laptop** (same-day rule after the memory-exhaustion reboot: heavy runs live on CI now). Design-side dated blocks (brief N2 correction — the burn DID need machinery, coinbase.rs pays fees to the miner — and the #367 early-construction record) follow design-side.
- **#342 payment URI + QR — DONE (2026-08-10, PR #345, Multica QUM-97): name-service-decision §3's T1 interim is COMPLETE** (contact book landed with #243; this is the second half). `qlab_wallet::uri` — the `qumbra:` payment-URI codec in the kernel crate (FFI-reusable), typed `UriError`, **integer-exact QMB↔bessel** (no float reachable; #303 is standing law on money), hand-rolled base64url/percent codecs, golden URI a **hard-coded 2,036-char literal** (the builder's own 4th commit killed the encode-through-encode self-lock). `qumbra_wallet::qr` — `qrcode` v0.14.1 (`default-features=false`, **zero transitive deps**, leaf crate only), Unicode half-blocks + hand-emitted SVG, capacity 2,953/2,954 boundary typed and **EC-L locked by the boundary test itself**. CLI: `address --uri/--amount-qmb/--label/--qr/--qr-svg`; `send --to` takes a `qumbra:` URI (colon-routed), URI amount prefills, **URI-vs-`--amount` disagreement is a hard error naming both values in both units**; memo/label display-only, marked NOT transmitted at every surface; the `qs1…` fingerprint travels beside every URI/QR (D3: a QR that merely scans is not a verified address). Judgment calls ratified at acceptance: u64-bessel upper bound (tail emission voids any finite supply constant), local `BESSEL_PER_QMB` + cross-lock test, lopsided-decimal refusal. **1,515/0/7 coordinator-independent, reconciled (1,492 implied baseline = 1,491@#336 + main's own +1; 23 new tests, none existing touched).** Open observation: physical v40-QR scannability unverified (no camera on the rig) — SVG is the likelier-to-scan artifact; a UX finding for the wallet briefs if it bites, not a defect here.
- **`qumbra-wallet history` — the party's own ledger (2026-08-10, PR #324, Multica QUM-91).** The third viewing layer built: a wallet's chronological transaction ledger, chain-derived (received / spent-via-on-chain-nullifier / send-events reconstructed from same-height spend+change), with a labeled `sends.v1` local record for the one thing the chain never carries — the recipient. **Task-book correction ratified (the join key): "join by tx/height" is unbuildable** (the nullifier wire is per-block not per-tx; build-time tip ≠ mined height), so the join runs on the **declared nullifiers** — exact, known at build time, and writable BEFORE `POST /v1/tx` answers (a failed POST no longer loses what you sent). UNAVAILABLE over a guessed fee when same-height grouping is ambiguous; change folded (not double-counted) with `in − out − fees == spendable` asserted. **1429/0/6.** Live-checked against the first user's real wallet; found #329 (self-sends trip the ambiguity refusal without a local record — minor, honest degradation).
- **🏁 THE FIRST USER JOURNEY — COMPLETE (2026-08-09 → 10): a real user went keygen → faucet grant → detect → open → SPEND → receive, end to end over the public edge, and the seven defects in the way were found by their real money and all fixed in one day.** The chain, in order: **#296** (faucet page lied — PR #306, placeholder derived+decoder-locked, `applied height` honesty rows) · **#308** (rate limiter keyed on the proxy = one global 24 h bucket — filed, fix owed) · **#310** (post-restart faucet re-funded spent notes and gave-up — PR #311, harvest subtracts on-chain nullifiers + typed `nullifier-spent` refusals + §9.1 gate) · **#309/#312** (the light-client scan never paged `/v1/compact`: a >1,024-block range silently truncated to page one and reported `complete/0` — **the ninth instance of the truncation-reads-as-complete pattern, the first found by live money**; PR #312, cap-agnostic paging with a progress guard) · **#313** (the committed AEAD payloads were served by no deployed surface — the `/v1/block/{h}/tx/{i}/full` route + the open path, #188 lane's serving+open; NodeRpc's side table retired for the same projection) · **#314** (a spent note stayed \"spendable\" under `complete` — PR #315, `GET /v1/nullifiers` per-block bulk-only + local spent-subtraction with the spend path's own derivation, `Option<u128>` making an unsubtracted figure unrepresentable). Suite spine across the day: **1324 → 1330 → 1340 → 1360**, every step reconciled to the function. **The first send itself: tx `5a3c0ee6…`, 1 QMB, fee 0.01, single real note + dummy slot — #219's latch working for a real user, proved in 3.62 s on the sender's laptop.** Final ledger, reconciled to the bessel: 40 QMB granted − 0.01 fee = 39.99 held across two addresses. **House-rule refinement ruled at PR #315: a change to existing RPC surfaces' bytes bumps `RPC_VERSION`; a pure route addition does not** — `Reader::version` is an equality check, so the 404-surfaced-as-named-UNAVAILABLE is the only capability probe that works across vintages (#275's grounds do not survive; the builder's on-record dissent was correct). Deploy side: three svc rolls in 24 h (`svc-4b60f8d` → `svc-f16bdad` → `svc-a0aa262`), each content-verified and read back, drift zero. Still open on this lane: #307 (receipt label), #308 (the limiter), and #188 4/4 (P2P wire discovery — the served surfaces are HTTP).
- **#297 wallet https — DONE (2026-08-09, PR #302, Multica QUM-81): issue #297 CLOSED, the last code gate on a stranger's first spend.** rustls (`ring` provider, `webpki-roots`, never native certs) as a **stream layer under the crate's existing hand-rolled HTTP/1.1** — no `ureq`/`hyper`; TLS reaches exactly one leaf crate (`qumbra-wallet`), and `qlab-cbserver` lends the scan flow via one additive `light_client_scan_with(fetch, …)` so no TLS enters the consensus build graph. https defaults `:443`, SNI = bare host, unknown scheme/path-in-authority refused by name, **no http-fallback-on-TLS-failure**, explicit `complete_io` so a certificate refusal names itself instead of racing the peer's reset (found against badssl, fixed pre-review). **1321/0/5-ignored on the merged tree (= 1299 + 14 + 8, reconciled); live: the issue's own repro command returns a real verdict against `seed.qumbra.org`.** The baton also caught the task-book's wrong premise (GET was a delegation into `qlab-cbserver`, `client.rs:730` held the second refusal) — asked on the issue, no reply, took the smaller separable action per the dispatch default. Disclosed gaps: no in-suite TLS-handshake positive (needs a test CA; `tests/live_edge.rs` is the opt-in probe), and **[#304](https://github.com/qumbra-labs/qumbra-lab/issues/304): `qumbra-ffi` is still plaintext-only** (iOS rung, own issue). Binary +1.45 MB (the compiled-in root program — priced, disclosed, accepted).

- **NEXT: the four T0 scenario drills, then M10 closes — but the sequence changed on 2026-07-29 and the new blocker is not a merge.** The redeploy that used to gate them is **done**: all four hosts run `t0-wan-2` (lab `main` `077b3ff`, carrying #79 + #82 + #86 + #87 + #91), rolled one host at a time. What now stands between here and the drills:
  - **The drills need `fid` on the wire, and `t0-wan-2` predates it** (#84 merged after the roll). Their pass/fail criteria are all of the form *"did the two halves finalize the same thing"*, and without checkpoint identity a drill proves the four nodes **agreed**, not that they were **right** — four nodes running one defect agree perfectly.
  - **Getting `fid` deployed means another roll, and a roll means a restart** — which is [#106](https://github.com/qumbra-labs/qumbra-lab/issues/106). node0's roll produced a ~4-block genesis fork (the node **mines before it has synced**), then a 1,716-block adoption, then a **53 m 49 s** main-thread wedge that recovered untouched. ~~T1-blocking on its own~~ — **the fork half is fixed and merged (item (1), PR #153): the roll is no longer gated on it.** What remains open on #106 is item (2), the wedge (cause never established — do not fix what nobody has diagnosed), and item (3), the `variants` correlation (eight samples cannot separate the two readings). Neither blocks a roll; both want the next image's instrumentation.
  - **[#107](https://github.com/qumbra-labs/qumbra-lab/issues/107)** — the main-loop period regressed 3–6× between the two images: t0-wan-1 reached its 30 s telemetry floor constantly, t0-wan-2 **never gets below 131 s** across 53 samples on four hosts. Mining is ruled out by measurement (difficulty is *lower* now), #87's three per-iteration calls are ruled out by inspection; the surface is `ratelimit.rs`, `transport.rs`, `addrman.rs`. Also means `quorum_ms` is currently **not separable from loop latency**, so there is no WAN quorum baseline.
  - **M11 remaining**: public-facing half of minimal explorer (opview + supply attestation 🟡 via #119/#126 — T0 operator view, not the full §6 explorer), join/mine docs, ops hardening. Faucet ✅ (#103), faucet listener ✅ (#128), peer discovery ✅ (#86), peer hardening ✅ (#99), halt-height drill pulled forward (#74), real-verifier gate closed early (T0-4). NAT traversal **deferred** (Larry, 2026-07-26 — T1 accepts outbound-only participants).
  - 🔴 **T1 still does not allow transacting.** Lab-side fact (unchanged): **no node in the deployed topology serves note discovery**, so no recipient of a transaction can find their output. ML-KEM discovery artifacts live only in `qlab_node::rpc::NodeRpc`'s in-memory side table; they are not in `StoredTx`, not in the block log, not on the P2P wire — and `qumbra-node` composes no `NodeRpc`. Coinbase is recoverable from chain data (#101); transaction outputs are not. **Design decision (DECIDED 2026-07-31): option 3, body-bound discovery** — see [`qumbra-design/t1-discovery-serving-decision.md`](https://github.com/qumbra-labs/qumbra-design/blob/main/t1-discovery-serving-decision.md) §10. Do not restate the options here; implement from the brief after the next-genesis rider lands. Related T1 gates this bullet does not absorb: [#130](https://github.com/qumbra-labs/qumbra-lab/issues/130) (state-machine desync; part (a) landed as PR #142, part (b) open) and [#134](https://github.com/qumbra-labs/qumbra-lab/issues/134) (joiner bans honest peers for serving correct history).
- Out of scope until the docs say otherwise: **recursion beyond rung 1** (rung 2 waits on rung-1 reality). *(Corrected 2026-07-29: this bullet also listed note encryption, networking and consensus, all three of which are DONE and logged above — M5/`qlab-note`, the M9 wave, M6 devnet. It was a stale scope fence contradicting its own milestone log in the same file.)*

## Bench discipline (non-negotiable)

1. Pin exact Plonky3-class revs in `Cargo.lock` before measuring anything; bench numbers against a moving prover are meaningless. First M1 task = select and pin.
2. Every recorded result carries: this repo's git rev, prover crate revs, hardware model, OS, power state (AC/battery, thermal).
3. A number is publishable to the design repo only after being reproduced twice on the same rig.
4. Calibrate the rig against published Poseidon2 numbers (benchmark-survey §2) before trusting any conservative-hash cell.
5. 🔴 **The acceptance bar is the full unfiltered workspace suite, serial:**

   ```sh
   cargo test --release --workspace -- --test-threads=1
   ```

   **Never a mode-scoped filter, and — amended 2026-08-01 — never a crate-scoped one either.**
   A filtered run structurally cannot see other modules' cross-checks. That first cost a stale
   width pin drifting across sessions until `PR #23`'s acceptance caught it, and the rule
   written then said `-p qlab-bench`, which **is itself a filter one level up**. `PR #166` paid
   for the gap: it reported `-p qlab-p2p` 198/0 and `-p qlab-devnet` 149/0, both honest, and the
   workspace suite failed on `n7soak::tests::s2_adversarial_rejected` — **in `qlab-bench`, a
   crate neither targeted run touches.** No crate-scoped run could have caught it.

   `--test-threads=1` is required, not stylistic: `qumbra-node` holds three real M3 prove tests
   and the release suite peaks 16–30 GB on a 36 GiB rig. **Never start a second one while one is
   running.**

   🔴 **Take the lock. It is not a courtesy queue — it is the only way any of the jobs finish.**

   ```sh
   scripts/rig run -- cargo test --release --workspace -- --test-threads=1
   ```

   `scripts/rig` is an actual mutex on this machine (`~/.qumbra-rig.lock`, created with `mkdir`,
   which is atomic). It waits for the holder, runs your command, and releases on exit — including
   on Ctrl-C and on a kill. `scripts/rig status` says who holds it. A holder whose process is gone
   is reaped automatically, so a crashed baton cannot wedge the rig.

   **This replaces "check `ps`, then post a START line on issue #64", and the reason is that the old
   rule could not work.** Check-then-act is a race, and issue #64 records it losing twice: two
   batons read the same "machine is free" and started 56 seconds apart, and separately the
   coordinator collided with a baton six minutes into its run. The thread's own remedy — *"re-check
   `ps` immediately before `cargo test`, not before writing the comment"* — narrows the window and
   does not close it. **On 2026-08-02 a measurement read 2.1× its true value** because a suite
   started underneath it; it was caught only because that baton knew what a contaminated run looks
   like.

   **It binds the coordinator too**, which the convention did not. The coordinator runs the most
   suites of anyone here and was outside the protocol that everyone else was being told to follow.

   **Issue #64 stays**, and its job is now the one it is actually good at: a human-readable log of
   what ran, what it measured, and what it cost. **It is no longer the mechanism.** Post there for
   the record, not for the lock.

   **If you must run heavy work outside the wrapper**, hold the lock around it yourself
   (`scripts/rig run -- bash -c '…'`) rather than skipping it. And if a release-mode number looks
   wrong, the cheap tell for contamination on this rig is **`sys` time inflated several-fold with
   instructions-retired nearly flat** — that pattern means something else was running, and the
   number should be discarded rather than caveated.

   **Enumerating is not accepting — two phases, two flag sets (added 2026-08-04, from the
   mint-combo baton).** Cargo stops at the first failing test *binary*, so a change with a wide
   blast radius reveals its breakage one crate per ~20-minute rig pass — that baton spent four
   passes listing what one would have shown. When you are *enumerating* what a change breaks,
   run the bar's command **plus `--no-fail-fast`** (still serial, still rig-locked). The
   **acceptance** run stays the unmodified command above, verbatim — the two phases answer
   different questions ("what is red" vs "is it green") and only the second is the bar.

   **Reconcile the arithmetic out loud**: your total must equal `main`'s baseline plus your new
   tests. Verify the negatives too — `FAILED` (case-sensitive), `panicked at`, `^error` all zero.
   Exit 0 from a launcher is not exit 0 from the suite. And **a `debug_assert` on a cross-layer
   agreement is a comment, not a check** — the bar is `--release`, where it compiles to nothing;
   promote such seams to `assert!` (precedent: the faucet's grant-cm seam, #253).

   **If you cannot run it, name the gap; do not imply a pass.** A stated "I did not run the
   workspace suite and here is what I did run" is fine and the coordinator re-runs at
   acceptance. A crate-scoped green reported as if it were the bar is not.

## Working conventions

- **Language pairing — run docs and findings ship EN + `-zh`; build notes do not.** The design repo pairs everything; this repo had paired nothing at the time the rule landed (88 docs, zero `-zh`; it is 101 docs with 5 pairs as of 2026-07-30) — which was never a decision, just an unstated default that drifted. The test is **who acts on it**:
  - **Paired**: run docs and evidence packs, findings/defect analyses, runbooks and operator procedures — anything Larry reads to make a call, or an operator reads under time pressure.
  - **Not paired**: build plans, per-baton working notes, scratch. Written by a session for a session; a translation is pure cost.
  - EN stays **authoritative** on technical detail (design-repo convention); the `-zh` is a translation, not a fork. Cross-link both ways at the top.
  - **Not retroactive.** Pair new docs and pair an old one when someone actually needs it. Back-filling 88 files would produce 88 translations nobody asked for and 88 more things to keep in sync.
- Non-trivial work: `git worktree add ../qumbra-lab-<slug> -b claude/<slug>`, land via PR to `main`, post the full PR URL, clean up the worktree after merge. Trivial single-file fixes may go direct to `main`.
- Layout: **20 crates** — see `README.md` for the annotated list; `crates/qlab-air` (the fixed-shape AIR) and `crates/qlab-bench` (harness) are the two an AIR change usually touches. `docs/` is scratch — polished results go to the design repo, not here.
- Rust 2021, workspace-managed. Keep `cargo check` green on every commit.
- Subagents where possible (research, parallel bench runs, doc translation); verify subagent-written files actually exist on disk (`git status` / `wc -l`) before trusting completion reports.
