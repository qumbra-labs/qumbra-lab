[workspace]
resolver = "2"
members = ["crates/qlab-air", "crates/qlab-bench", "crates/qlab-note", "crates/qlab-devnet", "crates/qlab-wallet", "crates/qlab-cbserver"]

[workspace.package]
edition = "2021"
license = "UNLICENSED"
publish = false

# Plonky3 deps intentionally NOT pinned yet — M1 starts by selecting and
# pinning exact revs (bench numbers are meaningless against a moving prover).
