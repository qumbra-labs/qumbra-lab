[package]
name = "qlab-air"
version = "0.0.0"
edition.workspace = true
publish.workspace = true

[dependencies]
# Pinned exactly at 0.6.2, matching qlab-bench (bench discipline).
# EXPERIMENT BRANCH (claude/mwhir-step0) — see qlab-bench/Cargo.toml.
p3-air = "=0.6.2"
p3-field = "=0.6.2"
p3-matrix = "=0.6.2"

[dev-dependencies]
# Published Keccak-f used as the authority the reference implementation
# is cross-checked against.
p3-keccak = "=0.6.2"
p3-koala-bear = "=0.6.2"
p3-symmetric = "=0.6.2"
